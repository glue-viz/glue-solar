
######## browser-ux

[P1.1] Browser search filters (date/text filter, scan in QThread with cancel)
  STATUS=not_implemented FEAS=small
  WHERE: glue-solar iris-observation-browser: glue_solar/sources/loaders/iris.py:205-211 (set_directory -> scan_directory synchronous), iris_loader.ui (only Folder/Change…/'Search subfolders'), glue_solar/sources/loaders/scan.py:118-179; reusable upstream: /Users/nabil/Git/glue-qt/glue_qt/utils/threading.py:
  REC: Ship the lazy version: one QLineEdit 'Filter' above the tree that setHidden()s top-level items whose STARTOBS/OBSID/Description text does not contain the string (covers '2014-03' style date filtering with zero extra widgets); add QDateEdit start/end only if someone asks. For the scan: wrap scan_directory in glue_qt.utils.Worker, add a `stop` callable kwarg to scan_directory wired to worker.isInterruptionRequested, and turn the progress bar busy + a 'Stop scan' button while running. ~40-60 lines plus two tests (filter hides rows; interrupted scan returns partial/empty list). Do not build a filename-pattern preset system.
  DOCFIX: Plan §Phase 1 item 1 (line 62-66) says the QThread is needed 'so large trees do not block the dialog' — measured 0.51 s for a 140-file real tree; only mirror-scale trees (thousands of files) block noticeably. Plan does not name the existing glue_qt.utils.Worker it should reuse. Plan claims this 'closes the no time-range search half' of the audit row: iris_xfiles' tstartval/tstopval search walks a level2/yyyy/mm/dd mirror by date; a start/end filter applied after a full scan is post-filtering, not search — fine for glue, but the row stays Partial unless the wording is adjusted. Audit line 167 ('No time-range search, filename-pattern presets, recent search windows, search cancellation') is accurate for the branch.

[P1.7] Log files (list *.log per observation, read-only text dialog)
  STATUS=not_implemented FEAS=trivial
  WHERE: glue-solar iris-observation-browser: glue_solar/sources/loaders/scan.py:145 (`if not name.endswith(_FITS): continue`), Observation dataclass scan.py:54-73 (no logs field), loaders/iris.py:229-235 (tree entries: sji/raster/sdo/archive only)
  REC: Value is near zero for anyone using public downloads, irispy sample data, or pooch caches (none ship .log); only pipeline mirrors have them. Either drop it (YAGNI) or do it in the same PR as P1.1 since both touch the tree-entry code: add `logs: list[Path]` to Observation, accept `name.endswith('.log')` keyed by _key_from_name in scan_directory (~5 lines), list them as non-checkable children, and on double-click show the text in a QPlainTextEdit(readOnly) QDialog (~15 lines) plus one test with a synthetic .log. ~25-30 lines total.
  DOCFIX: Plan line 36/90 and audit line 170 are correct that nothing is implemented, but neither says that IRIS Level 2 public downloads, irispy test/sample data and pooch caches contain no *.log files — they are level1to2 pipeline by-products (`iris_l2_<ts>_<obsid>[_raster|_sji].log`) present only on local mirrors, so 'trivial' is right on size but the feature has almost no users. Audit line 57 ('Internal iris_xfiles log window: shows the text contents of a selected log file') is accurate; note iris_xfiles has no dedicated log search or file-type entry — it only displays a picked file that matches '.*log'.

  VERIFY: Both verdicts stand: P1.1 and P1.7 are not implemented on iris-observation-browser, nothing on the glue/glue-qt fork branches touches them, and the cited file:line references, upstream helpers (glue_qt.utils.Worker, no cancel API but QThread interruption works cooperatively — reproduced), test results (23 passed) and timing (0.50 s for 130 files) all reproduce. Additions/corrections to the auditor's report: (1) P1.1 size understated on the test side — six existing test_importer.py tests construct the dialog and read the tree synchronously, and the recursive-toggle and extract-archive tests re-enter set_directory, so a QThread scan means ~6 test edits (qtbot.waitUntil) or a synchronous path kept for tests; code size (~40-60 lines) is fair. (2) P1.7 'no public .log' claim is now backed by a public archive listing, not just the HCR page: sdc.uio.no level2 directories for three observations (368/18/12 files) contain zero log files; pooch tarballs contain only .fits. The pipeline source is at ssw/iris/idl/uio/level1to2/ (auditor's path was vague/404 elsewhere) and also emits '.errorlog', which an implementation should accept alongside '.log'. (3) The auditor's aside that 'importing glue

######## viewer-composition

[P1.2] Viewer presets: Whisker and Spectroheliogram Image Viewers
  STATUS=not_implemented FEAS=trivial
  WHERE: glue-solar iris-observation-browser: glue_solar/sources/iris.py:48 is the only viewer-creation call (app.new_data_viewer(ImageViewer, data=dialog.first_image)); no x_att/y_att/slices code anywhere (grep of glue_solar/ and docs/ for whisker|spectroheliogram|x_att found nothing)
  REC: Ship only the Whisker preset (~10 lines: new_data_viewer + x_att/y_att + slices at the requested slit index, or just leave the slit slider at 0 and let the user drag it). Do not build a Spectroheliogram preset: it is the default Image Viewer layout for every raster window; document that instead. Skip a slit-position dialog in v1 (the slit slider appears automatically).
  DOCFIX: Plan P1.2 and audit rows 'Spectroheliogram panels'/'Whisker plot' present both as needing a preset; the spectroheliogram view is what glue opens by default for a raster window (x=wavelength, y=slit, slider=raster position), so only the whisker needs code. Plan's 'a few lines each' is confirmed.

[P1.3] Quicklook layout helper (load raster+SJI, HPC links, SJI + raster-map + Profile viewers)
  STATUS=not_implemented FEAS=small
  WHERE: glue-solar iris-observation-browser: glue_solar/sources/iris.py:31-48 browse_iris loads via QtIRISImporter (dialog.datasets, dialog.first_image) and opens one ImageViewer; no links, no map viewer, no profile viewer
  REC: Extend browse_iris (not a new action): after app.add_datasets, call the P1.4 link function on the loaded SJI/raster datasets, then open the three viewers (~25 lines total) with profile function set to 'mean'. State honestly in the docs that SJI-drawn ROIs will not propagate to the raster until Phase 3.1/3.2; raster-drawn ones and world-coordinate subsets do.
  DOCFIX: Plan P1.3 'the honest subset of iris_raster_browser that Glue's linking already supports' overstates it: with the HPC world links, glue cannot invert the SJI gWCS (lon/lat depend on the time pixel: axis_correlation_matrix rows all ones), so image-viewer ROIs on the SJI raise IncompatibleAttribute on the raster; only raster->SJI ROI propagation and world-coordinate subsets work.

[P1.4] Explicit link helper (programmatic HPC Longitude/Latitude LinkSame)
  STATUS=not_implemented FEAS=trivial
  WHERE: glue-solar iris-observation-browser: nothing (grep LinkSame|ComponentLink|add_link in glue_solar/ -> no hits); docs/user_guide/loading-iris-level-2-raster-and-sji-data.rst:57-59 tells users to pair the components by hand
  REC: Write one ~10-line function link_hpc(datasets) that pairs world cids by physical type with LinkSame, call it from browse_iris automatically when both an SJI and a raster are loaded, and also expose it as a menubar action for File->Open loads. Keep it after Phase 3.1 lands; change the plan text from 'deleted once autolink lands' to 'kept; autolink adds the pixel link'.
  DOCFIX: (1) docs/user_guide/loading-iris-level-2-raster-and-sji-data.rst:57-59 'manually pair Helioprojective Longitude and Helioprojective Latitude between datasets' — the SJI side is labelled 'Longitude'/'Latitude' in the link editor. (2) Plan P1.4 'deleted once Phase 3's autolink work lands upstream', plan P3.1 'This single change makes IRIS autolinking work and removes Phase 1 item 4', suggested-order row 2 'deletes Phase 1.4', and APE14_AUTOLINK_TODO.md §4 bullet 3 'Delete explicit link helper ... once the released glue autolinks IRIS data' are all wrong: the two link kinds cover different subset directions. (3) Plan/audit call the SJI WCS 'gWCS' correctly (type module gwcs.wcs), fine.

[APE14-TODO-4.2] _GlueWCS world_axis_object_classes coherence after arcsec conversion (APE14_AUTOLINK_TODO §4 bullet 2)
  STATUS=partial FEAS=trivial
  WHERE: glue-solar glue_solar/sources/loaders/iris.py:35-69 (_GlueWCS overrides names/units/values only; world_axis_object_classes inherited from the wrapped WCS)
  REC: In _GlueWCS override world_axis_object_classes to rewrite the celestial 'unit' kwarg to arcsec (3 lines), with a test that HighLevelWCSWrapper(_GlueWCS(raster)).pixel_to_world returns a SkyCoord in arcsec matching the low-level values. Do it in the glue-solar PR that adds link_hpc, independent of the upstream autolink PR.
  DOCFIX: APE14_AUTOLINK_TODO.md §4 bullet 2 is still open and the answer is 'not coherent for the raster'; the plan does not mention it.

  VERIFY: All four statuses hold (nothing in glue-solar; stock glue 1.27 APIs as described; fork tests 31 passed/1 xfailed/1 xpassed; stock autolink 0 links). I refuted two pieces of the auditor's reasoning that change the plan: (1) 'neither link kind is time-aware, coordinated browsing waits for Phase 3.2' — on stock glue, LinkSame(lon,lat) + ComponentLink(raster Time -> SJI 'Time (Utc)' world cid) + a _GlueWCS subclass for the SJI with an all-ones axis_correlation_matrix gives time-aware SJI-ROI -> raster propagation (186/187 raster steps vs 14/187 for the fork's frame-0 WCSLink; derived (x,y,t) equals the direct gWCS inverse), ~20 lines in glue-solar; root cause is glue's dependent_axes (coordinate_helpers.py:163-175) reading the forward correlation matrix for the inverse direction, not an uninvertible gWCS. Only slider synchronization remains upstream Phase 3.2; the plan's 'time-nearest join primitive' in glue-core is unnecessary for subsets. (2) 'fork WCSLink gives no raster->SJI' — it does (18600 px for steps 0-15); what it gives is frame-0 geometry both ways, which on this real rotation-tracked 4.8 h sit-and-stare (XCENIX drift 42.2 arcsec) reaches only raster steps 0-13 — ~250 full-f
   X P1.3: Status (not_implemented), size (small) and the extend-browse_iris recommendation stand; two things are wrong. (1) 'glue cannot invert the SJI gWCS (lon/lat depend on the time pixel)' and 'SJI-drawn ROIs will not propagate to the raster until Phase 3.1/3.2' are wrong: irispy's gWCS inverse is time-aware (world_to_pixel_values(lon61,lat61,t61) -> (10,10,61)); the block is on glue's side — glue/core/coordinate_helpers.py:163-175 dependent_axes reads the *forward* axis_correlation_matrix (time row [0,0,1]) so the world->pixel link for SJI x,y never receives world time and CoordinateComponentLink zero-fills it (frame-0 geometry), and the raster has no SJI time cid at all (hence IncompatibleAttrib
   X P1.4: Status (not_implemented), feasibility (trivial), the physical-type matching code and the conclusion 'P1.4 is not obsoleted by ape14-wcs-autolink' all stand and the conclusion is stronger than stated. Two evidence claims are wrong. (a) 'Fork WCSLink: raster ROI->SJI 0 px' is not a directional limitation: the fork link is bidirectional — a raster ROI on steps 0-15 reaches 18600/91760 SJI px; steps 40-120 give 0 only because the frame-0 geometry maps raster steps >= 14 outside the SJI frame-0 FOV (a whole-frame SJI ROI reaches raster steps 0-13 of 187). Correct framing: the fork link is exact frame-0 geometry in BOTH directions; LinkSame is per-frame exact for raster->SJI and world subsets, and

######## network-context

[P1.5] GOES context (Fido GOES XRS light curve for the observation interval as a 1D glue dataset)
  STATUS=not_implemented FEAS=small
  WHERE: glue-solar iris-observation-browser: nothing (grep -i goes/fido/timeseries over glue_solar/ returns only nothing); upstream pieces: sunpy.net.Fido + sunpy.net.dataretriever.sources.goes.XRSClient, sunpy.timeseries.sources.goes.XRSTimeSeries, glue.core.component.DateTimeComponent, glue_qt.viewers.sca
  REC: Implement as a ~40-line menubar_plugin ('IRIS: GOES context…') acting on the selected IRIS dataset's meta STARTOBS/ENDOBS: Fido.search(a.Time(start,end), a.Instrument.xrs, a.Resolution.avg1m) -> Fido.fetch -> TimeSeries(files, concatenate=True).truncate(start,end).to_dataframe() -> Data(time=index.values, xrsa, xrsb) -> ScatterViewer(x=time, y=xrsb, y_log). Wrap the whole thing in try/except showing a QMessageBox. Add sunpy[timeseries] to pyproject; test with monkeypatched Fido/TimeSeries. Keep it out of the browser dialog.
  DOCFIX: Plan §1.5 'shown in a Profile/Scatter viewer': the Profile viewer cannot plot a non-coordinate time component (glue/viewers/profile/state.py:69-71); say Scatter viewer. Plan/audit say nothing about the missing `sunpy[timeseries]` extra, which is a hard prerequisite (pyproject.toml:21 has only sunpy[map,net]). Plan's 'Suggested order' step 3 lists 1.5 as an 'independent nice-to-have' — correct, it has no dependency on #44 or Phase 3.

[P2.4] SDO pointing context (Fido AIA/HMI fetch + propagate_with_solar_surface, IRIS FOV polygon overlay)
  STATUS=partial FEAS=medium
  WHERE: glue-solar iris-observation-browser: glue_solar/sources/loaders/scan.py:68,111-115,174-175 (obs.sdo = aligned aia_l2_* cutouts), loaders/iris.py:110-134 (image_data/_image_cube_data load AIA cutouts through irispy AIACube with sdoaia cmap and HPC world axes), glue_solar/sources/maps.py:19-41 (_parse
  REC: Ship the sunpy-only recipe proven in demo_sdo.py as one action ('IRIS: SDO context image…') on a loaded SJI/raster Data: Fido.search(a.Time(t, t+1min, near=t), a.Instrument.aia, a.Wavelength(171 AA)) -> Map -> submap around the IRIS FOV corners (+margin) -> _parse_sunpy_map (already in maps.py) -> add Data; plus a 5-point FOV polygon Data and a line scatter layer on the AIA Image viewer. Keep propagate_with_solar_surface only as the `with` guard around the corner transform / reproject (it is a context manager, not a function). Do it after Phase 1.4/3.1 so the link exists; ~60-80 lines + mocked-Fido tests. Do not port irispy coalign/01 (aiapy + sunkit_image cross-correlation) — the loaded aia_l2 cutouts already are the co-aligned product.
  DOCFIX: Audit row 'SDO pointing context ... no live SDO retrieval, rotation, limb context, or IRIS field-of-view overlay': 'rotation' is not a gap for the aligned cutouts (they are per-exposure time-matched); the real gaps are limb/full-disk context and the FOV polygon. Plan §2.4 'Fido AIA/HMI fetch + propagate_with_solar_surface': propagate_with_solar_surface is a context manager (`with propagate_with_solar_surface(): ...`), and for a co-temporal image found with a.Time(near=) it changes nothing measurable — it only matters if an image hours away is reused; the irispy coalign examples do not use it. Plan should state that the irispy coalign gallery is a cross-correlation recipe requiring aiapy + sunkit_image (not installed, not dependencies) and is not the retrieval recipe to wrap. Plan should note the naming mismatch (Hpln/Hplt vs Helioprojective Longitude/Latitude) that the link helper must h

  VERIFY: Both items' statuses hold (P1.5 not implemented; P2.4 partial: browser loads LMSAL-aligned aia_l2 cutouts, nothing for live fetch/FOV). P1.5 verdict agreed, but the auditor missed two glue-core 1.27.0 datetime defects that hit the proposed Scatter plot directly: (1) datetime axis tick labels are wrong (glue/utils/matplotlib.py datetime64_to_mpl uses the pre-3.3 0001-01-01 epoch, no set_epoch anywhere; num2date(xlim) gives 3990-09-06 for 2021-09-05 data) -- set rcParams['date.epoch']='0000-12-31T00:00:00' or plot elapsed minutes; (2) the Profile viewer does not just hide the time column, add_data raises in _reset_y_limits (state.py:227) for a datetime-first Data, and the auditor's reported 'profile x_att choices' output cannot have come from its script. sunpy[timeseries] (h5netcdf) prerequisite, satellite iteration, Scraper network-on-search, Fido/attrs signatures all re-verified. P2.4 disagreed on the linking premise: IRIS SJI labels are 'Longitude/Latitude' (irispy names), rasters 'Helioprojective Longitude/Latitude', sunpy Map 'Hpln/Hplt' -- three namings, but all share world_axis_physical_types, and the glue fork's ape14-wcs-autolink branch already autolinks SJI/raster/sunpy-Map
   X P2.4: Status 'partial', the executed recipe and the doc corrections about irispy/propagate are right, but blocker (1) and the linking premise are wrong. (a) Naming: IRIS SJI Data world labels are 'Longitude'/'Latitude'/'Time (Utc)' (irispy's own axis names win; the loaders/iris.py:27-32 _AXIS_NAMES fallback only fires for FITS-WCS rasters, which come out 'Helioprojective Longitude/Latitude'); sunpy-Map Data is 'Hpln'/'Hplt'. Three namings, not two -- but linking must not be label-based at all: all three expose identical world_axis_physical_types ('custom:pos.helioprojective.lon/lat'), and the glue fork's ape14-wcs-autolink branch (016d9cf2) already autolinks SJI<->sunpy-Map AIA<->raster with no gl

######## slit-overlay

[P1.6] Slit overlay on SJI
  STATUS=not_implemented FEAS=small
  WHERE: glue-solar iris-observation-browser: glue_solar/sources/loaders/iris.py:72-86 (_cube_data keeps only the 'time' extra coord), :110-114 (_image_cube_data); nothing draws a slit. Plan IRIS_GLUE_GAP_PLAN.md:86-89; audit IRIS_IDL_GLUE_CAPABILITY_AUDIT.md:78,171
  REC: Implement as a glue subset on the SJI dataset, not a linked scatter dataset: in `_image_cube_data`, when `'slit x position' in cube.extra_coords.keys()`, add a broadcast 'Slit x' component (pixel units; ignore irispy's arcsec label) and `dc.new_subset_group('Slit', (px > slitx-0.5) & (px <= slitx+0.5))` from the menubar/browser action (~15 lines + one test). Keep the raster -TAB projection (`sji.coords.world_to_pixel_values(lon, lat, t_world)` per nearest-time frame, needs P1.6-dep fix) only as the fallback when there is no aux table. Reword the plan/table accordingly.
  DOCFIX: IRIS_GLUE_GAP_PLAN.md:86-89 'Build a small scatter dataset of the raster slit positions (already available from the -TAB WCS) and add it as a layer on the SJI viewer, linked through the HPC components. No glue-qt change needed.' -> -TAB claim is correct (irispy 0.8.1), but: HPC-linked scatter is disabled by glue unless time is also linked and then mispositioned (glue-core coordinate_helpers bug); a scatter layer is never sliced by the SJI time slider so it draws every frame's slit at once; the simpler source is the SJI aux SLTPX1IX/SLTPX2IX already exposed as irispy extra_coords. IRIS_GLUE_GAP_PLAN.md:37 'Slit-overlay dataset in glue-solar' -> should be a subset on the SJI dataset. docs/user_guide/loading-iris-level-2-raster-and-sji-data.rst:57-59 'manually pair Helioprojective Longitude and Helioprojective Latitude between datasets' -> SJI world components are labelled 'Longitude'/'Lati

[P1.6-dep] World (HPC) links into the SJI gWCS derive pixels with time frozen at frame 0 (glue-core bug; 3-line workaround in glue-solar)
  STATUS=not_implemented FEAS=trivial
  WHERE: glue-core 1.27.0 glue/core/coordinate_helpers.py:104 (`world_dep = wcs.axis_correlation_matrix[:, pixel_axis]` collapses any world input whose row is 0 to `.flat[0]`), vs :163 dependent_axes (correct closure). Workaround location: glue_solar/sources/loaders/iris.py `_GlueWCS` (:34-69). Fork branches
  REC: Add an `axis_correlation_matrix` property on `_GlueWCS` returning the transitive closure of the wrapped matrix (verified 3 lines), with a test on the SNS SJI. File a glue-core issue/PR for coordinate_helpers.py:104. This is a prerequisite for any world-linked overlay onto the SJI (P1.3 quicklook, P1.4 link helper, P1.6 fallback); a raster->SJI link additionally needs a time component in the SJI gWCS 'seconds since epoch' convention, which the link helper must synthesise.
  DOCFIX: IRIS_GLUE_GAP_PLAN.md:80-83 (P1.4 'creates the HPC longitude/latitude links programmatically ... Removes the most error-prone manual step') and :74-77 (P1.3 'creates the manual HPC links') assume lon/lat links suffice; for the SJI viewer direction they do not (time dependency + glue-core bug). Audit :180 'SJI and raster temporal coordination ... Datasets can be linked manually by available coordinates' is overstated for the raster->SJI direction.

  VERIFY: Both items' core conclusions survive (P1.6 not implemented, small, do it as a per-frame subset on the SJI from SLTPX1IX; P1.6-dep is a genuine glue-core bug at coordinate_helpers.py:104 with no upstream issue/PR and no fork commits). Refuted/corrected with evidence: (a) the 'crop' story is wrong — irispy test SJIs are stride-10 decimations and the 0.8.1 SNS SJI header is self-inconsistent (CDELT unscaled; repaired in irispy main bc5e39e), so the auditor's projected slit x≈21 numbers are artefacts; on repaired files aux SLTPX1IX/10 (18.7) and the raster -TAB projection (19.5-19.7) agree to ~1 px, which is the real basis for 'either source works'; (b) the 3-line axis_correlation_matrix override is unsafe — it makes glue-qt ImageViewer.format_coord raise OverflowError in astropy WCSAxes on every mouse move; a verified narrower fix is a ~10-line wrap of world2pixel_single_axis (and its imported name in component_link), i.e. a glue-core monkeypatch, so P1.6-dep is 'small/hacky, belongs upstream', not 'trivial'; (c) P1.6-dep is not a prerequisite for P1.3/P1.4 as scoped: with lon/lat-only links, raster-drawn subsets already display on the SJI viewer today; only SJI-drawn subsets fail on 
   X P1.6: Status (not implemented), size (small) and the core recommendation (per-frame subset on the SJI built from SLTPX1IX, not an HPC-linked scatter) HOLD. Corrections to evidence/doc text: (1) irispy test SJIs are not 'crops' — they are stride-10 DECIMATIONS (irispy/data/test/compress.py STRIDE=10, CRPIX rescaled); aux SLTPX values are original-resolution indices and SLTPX1IX/10 lands in range (18.7 for SNS NAXIS1=37; 54.9/126.9 for the 2023 NAXIS1=178 files). A unit test on the real file can assert the component/subset state without synthetic values. (2) The auditor's projected slit x=21.05/20.62/21.24 are artefacts of a self-inconsistent 0.8.1 test header (CDELT1=0.16635 left unscaled while CRP
   X P1.6-dep: The BUG is real and reproduced (coordinate_helpers.py:104 collapses uncorrelated world inputs to .flat[0]; derived SJI x 21.05/144.96/275.06 vs direct 21.05/20.62/21.24). But the recommended fix is NOT safe and the size is not 'trivial': overriding _GlueWCS.axis_correlation_matrix with the all-True closure breaks the glue-qt ImageViewer coordinate readout — axes.format_coord(18,20) raises OverflowError ('cannot convert float infinity to integer') in astropy/visualization/wcsaxes/formatter_locator.py:642 because WCSAxes now treats the time axis as correlated with the image plane where it has zero range (spacing=inf); format_coord runs on every mouse move. Unpatched it returns '−53" −400" (wor

######## science-moments

[P2.1] Moment maps dialog wrapping irispy.utils.moments.calculate_moments
  STATUS=not_implemented FEAS=small
  WHERE: glue-solar: nothing (grep -rn moment glue_solar docs -> 0 hits). Upstream: /Users/nabil/Git/glue-solar/.venv/lib/python3.14/site-packages/irispy/utils/moments.py:33-209 (irispy-lmsal 0.8.1); rest wavelength in irispy/meta.py:194-198
  REC: Build as one module glue_solar/sources/moments.py (~100-150 lines + ~40-line test): register with glue_qt.config.layer_action('IRIS: line moments…', single=True, data=True) (right-click on the dataset; callback(layer, data_collection)) instead of a menubar entry plus dataset picker; QFormLayout with rest wavelength (A, prefilled from cube.meta.rest_wavelength, editable), wings low/high (A, blank = whole window), min intensity, saturation limit, integrated checkbox. Produce ONE Data per run: d = _cube_data(first_map, f'{label}-moments') then d.add_component(Component(np.asarray(c.data), units=str(c.unit)), name) for all 5 (verified: 8 components incl. mask and Time), append to the data collection, add two pixel LinkSame links to the source, open an ImageViewer on 'velocity'. Test with an explicit in-window rest wavelength.
  DOCFIX: PLAN 2.1 / AUDIT: 'released in irispy-lmsal 0.8.1' — calculate_moments (and red_blue, density) were added in irispy 0.7.0 (2026-05-07, PRs #124/#125/#126; git tag --contains 3b9a506 -> v0.7.0); RBA API was simplified in 0.8.0 (#144). Harmless because glue-solar pins >=0.8.1, but say 'since 0.7.0'. PLAN 2.1 'add the maps as datasets linked to the source' — glue-core 1.27 wcs_autolink returns [] for every pairing involving these datasets; links must be created explicitly (pixel LinkSame), and 'maps as datasets' should be 'one dataset with five components'. PLAN 2.1 omits that the 4D stack datasets are plain ndcube.NDCube with dict meta and cannot be passed to calculate_moments as-is (see P2.1-stack).

[P2.1-prereq] Loader keeps a handle to the source SpectrogramCube on the Data
  STATUS=not_implemented FEAS=trivial
  WHERE: /Users/nabil/Git/glue-solar/glue_solar/sources/loaders/iris.py:72-85 (_cube_data) and :93-107 (_raster_collection_data)
  REC: One line in _cube_data: data.irispy_cube = cube. In the stack branch of _raster_collection_data add data.irispy_sequence = sequence (or store the sequence instead) so a stack can be re-wrapped with the scan-0 SGMeta. Skip WeakKeyDictionary/registry: the Data already owns the arrays, the cube adds only WCS+meta, both already referenced by the Data.
  DOCFIX: None; the plan correctly states the handle is missing.

[P2.1-link] Maps as components on the same Data vs separate Data, and how to link
  STATUS=non_goal FEAS=trivial
  WHERE: glue-core 1.27 glue/core/data.py:1040-1110 (add_component shape check), glue/core/link_helpers.py LinkSame, glue/plugins/wcs_autolinking
  REC: Separate 2D Data (one per moments run, five components) plus two pixel LinkSame links — three lines, exact, and the link chain will carry over to the SJI once raster<->SJI world links exist (Phase 1.4/3.1). Do not use the broadcast-component trick.
  DOCFIX: PLAN 2.1 should say 'one dataset per run, maps as components, pixel-linked to the source' rather than 'maps as datasets linked to the source' and should not imply autolinking.

[P2.1-stack] Moments on the optional 4D stacked dataset
  STATUS=partial FEAS=small
  WHERE: /Users/nabil/Git/glue-solar/glue_solar/sources/loaders/stack_spectrograms.py:74-84 (returns ndcube.NDCube with meta=dict(...)); irispy/spectrograph.py:188-196 (wavelength_axis from array_axis_physical_types)
  REC: In the moments action: if the handle is an NDCube, wrap it in SpectrogramCube as above and take rest_wavelength from the kept sequence[0].meta; link with three pixel LinkSame links (scan, y, x). Or explicitly scope v1 to single-scan datasets and document it — either is a few lines.
  DOCFIX: PLAN 2.1 does not mention that stacked datasets are NDCube, not SpectrogramCube; state that v1 covers per-scan datasets or add the wrap.

[P2.3a] calculate_red_blue_asymmetry wrapper
  STATUS=not_implemented FEAS=small
  WHERE: Upstream only: irispy/utils/red_blue.py:122-412 (installed 0.8.1 == git checkout, diff -q same)
  REC: After P2.1 lands: second layer_action reusing the same form scaffolding; call with return_profiles=False (the profile cubes are the source re-labelled in velocity and a spline resample — no value inside glue) and put asymmetry + quality as two components on one 2D Data with the same two pixel LinkSame links; run in a QThread or accept a busy cursor for large rasters.
  DOCFIX: PLAN 2.3 'both cube-in/cube-out like calculate_moments' is true for RBA only; note that the default return_profiles=True also yields 3D profile cubes, and that the per-pixel loop is slow (~40 us/px), unlike moments.

[P2.3b] density_diagnostic wrapper
  STATUS=not_implemented FEAS=medium
  WHERE: Upstream only: irispy/utils/density.py:99-320 (installed 0.8.1 differs from checkout only in the fiasco import helper); recipe /Users/nabil/Git/irispy/examples/analysis/06_oiv_density_diagnostics.py
  REC: Remove density from the 'near-zero cost, one entry each' framing. Either drop it from the plan or list it under 'on demonstrated demand' with an explicit 'density' extra (fiasco) and a dialog that takes two already-computed intensity components as inputs.
  DOCFIX: PLAN 2.3: 'density_diagnostic (O IV), both cube-in/cube-out like calculate_moments' is wrong — it is arrays-in/dict-out, requires fiasco (uninstalled, undeclared) and two line-intensity maps; 'one entry each ... at near-zero cost' does not apply to it.

  VERIFY: Re-ran every decisive check independently (scripts: scratchpad/verify_moments.py, verify_moments2.py, verify_fork_link.py, verify_fork_link2.py). All six items' status/evidence/feasibility/doc_corrections hold; no refutations. Findings the auditor missed or under-stated: (1) glue-core 1.27 wcs_autolink returns [] for glue-solar datasets because the loader's `_GlueWCS` is a BaseLowLevelWCS and wcs_autolinking.py:320-321 keeps only `isinstance(data.coords, BaseHighLevelWCS)` — glue-solar datasets are filtered out before any WCSLink is attempted; wrapping them in HighLevelWCSWrapper breaks glue (`'HighLevelWCSWrapper' object has no attribute 'world_axis_names'`), so this cannot be fixed in glue-solar alone. On the fork ape14-wcs-autolink (accepts BaseLowLevelWCS at :430) the 2D moment map DOES autolink to the SJI (WCSLink returned), but src3D<->map2D and map2D<->map2D still raise IncompatibleWCS — so the recommended two pixel LinkSame links stay necessary even after Phase 3.1; the plan's 'linked to the source' must be explicit. (2) The auditor's recipe `_cube_data(first_map, ...)` + add_component for all 5 stores the intensity array twice (components 'maps' and 'intensity'); build the

######## science-fitting

[P2.2] Gaussian fit maps (single, then double) via astropy parallel_fit_dask
  STATUS=not_implemented FEAS=medium
  WHERE: glue-solar: nothing (grep 'parallel_fit|gaussian|modeling|dask' over glue_solar/ and docs/ is empty; no fit branch among local/remote branches). Upstream: astropy.modeling._fitting_parallel.parallel_fit_dask (installed 8.0.1, /Users/nabil/Git/glue-solar/.venv/lib/python3.14/site-packages/astropy/mod
  REC: Wrap it: one pure function fit_gaussians(data, wavelength_nm, n_gaussians=1|2, rest_nm) -> dict of 2D arrays + residual cube (~60 lines, plain floats, seed from spatial mean like the examples but fall back to argmax when rest is outside the window), run in glue_qt.utils.threading.Worker (existing QThread helper, glue-qt main since 2023, /Users/nabil/Git/glue-qt/glue_qt/utils/threading.py) with scheduler='processes', build 2D Data objects with the sliced spatial WCS and link them to the source. Do single first; double is the same path with a different seed list. Roughly 150-250 lines including tests; add a synthetic Gaussian cube fixture since the real test rasters cannot produce a physical fit.
  DOCFIX: PLAN line 124 'Requires `dask` - add as an optional `fitting` extra.' is wrong: irispy-lmsal 0.8.1 (glue-solar hard dep, pyproject line 22) itself requires dask[array]>=2024.7.0 (irispy v0.8.1 pyproject.toml line 36; pip shows irispy-lmsal Requires: dask), so dask is always installed; no extra. Also no astropy pin needed: parallel_fit_dask is astropy>=7.1.0 and irispy 0.8.1 already pins astropy>=7.2.0. PLAN lines 119-120 'with percentile background seeding' describes irispy main, not v0.8.1: the 20-line percentile-seeding block in 01_spectral_fitting.py is only on main (git diff v0.8.1 main); harmless since it is documentation. PLAN line 122 'run in a background thread' is right but under-specified: dask's default 'threads' does NOT speed this up (13.4 s vs 13.6 s, GIL) and compute() is a single blocking call so a processEvents loop cannot work; a QThread (glue_qt.utils.threading.Worker)

[P2.2-threading] Background execution for long fits
  STATUS=available_upstream FEAS=small
  WHERE: /Users/nabil/Git/glue-qt/glue_qt/utils/threading.py (Worker(QThread): result/error signals; in glue-qt main, last changed d65c3fe9 2023-08-11); glue_qt/viewers/matplotlib/compute_worker.py is the queue-based variant used by profile/histogram viewers.
  REC: Use glue_qt.utils.threading.Worker(fit_fn, ...) and connect result/error to slots that add the Data on the main thread; pass scheduler='processes' for real rasters, 'single-threaded' in tests.
  DOCFIX: PLAN line 122-123 says 'run in a background thread' without naming a mechanism; state Worker + scheduler='processes' explicitly and drop any implicit expectation of a cancel button or progress bar for the fit.

[P2.2-dask-extra] 'fitting' extra with dask
  STATUS=non_goal FEAS=n/a
  WHERE: irispy v0.8.1 pyproject.toml line 36 'dask[array]>=2024.7.0' (hard dependency); glue-solar pyproject.toml line 22 'irispy-lmsal>=0.8.1'
  REC: Drop the extra from the plan; dask arrives with irispy.
  DOCFIX: PLAN line 124 'Requires `dask` - add as an optional `fitting` extra' - remove.

[P2.2-irispy-helper] Does irispy ship a fitting helper?
  STATUS=not_implemented FEAS=n/a
  WHERE: /Users/nabil/Git/irispy: git grep parallel_fit_dask v0.8.1 / main -> only examples/analysis/01 and 07; irispy/utils/utils.py:90 gaussian1d_on_linear_bg (@custom_model, no callers at v0.8.1 or main)
  REC: Keep the recipe in glue-solar (or contribute a cube-in/maps-out fit_gaussian to irispy later, mirroring calculate_moments). Do not reuse gaussian1d_on_linear_bg: its 'standard_deviation' is sqrt(2)*sigma, which would silently change the width maps.
  DOCFIX: PLAN line 116-118 'wraps astropy' is accurate; audit line 180 accurate.

  VERIFY: Auditor's verdicts hold: P2.2 is not implemented anywhere (glue-solar, fork branches, irispy), parallel_fit_dask in astropy 8.0.1 does the job, dask is a hard irispy dependency so the 'fitting' extra in PLAN line 124 is wrong, the cube-handle prerequisite (PLAN 113-115) is unnecessary for fitting, threads do not help and a Worker QThread around scheduler='processes' is the workable form. Corrections: parallel_fit_dask dates from astropy 7.0.0 (#16696), not 7.1.0; the 2.3x process speedup and 23 s double-Gaussian numbers are bad-seed artefacts (in-window seeds: single 6.4 s -> 4.6 s with processes, double 8.3 s single-threaded for 7480 spectra), so the plan should budget ~1 ms/spectrum and say seeding (argmax of spatial mean, not the gallery's fixed rest wavelength) is the correctness- and speed-critical part; data.coords.pixel_to_world_values needs the wavelength pixel index as the FIRST argument (Cartesian order) - the auditor's snippet omitted this and the wrong order silently yields a constant wavelength. Missed by the auditor: glue-core 1.27.0 already ships glue.core.fitters (SimpleAstropyGaussianFitter, GaussianFitter, PolynomialFitter) and the glue-qt Profile viewer's fit too
   X P2.2: Conclusions (not_implemented, medium, no cube handle needed, dask/extra corrections, test-data caveat) all hold; three facts in the evidence are wrong or overstated. (1) parallel_fit_dask was added in astropy v7.0.0 (astropy PR #16696 'Add function to carry out parallel fitting on N-dimensional datasets', milestone v7.0.0, merged 2024-09-03; v7.0.0 CHANGES.rst line 64), not 7.1.0 - PR #17538/#18065 only added fit_info. Immaterial for the plan since irispy pins astropy>=7.2.0. (2) 'processes ~2.3x faster' and 'double Gaussian 23 s' are seed artefacts, not properties of the recipe: re-run on the same sns Si IV 1403 (187,40,29) cube with an in-window argmax seed gives 6.38 s single-threaded / 6

######## ape14-autolink

[P3.1] APE-14 WCSLink / wcs_autolink support in glue-core (branch ape14-wcs-autolink)
  STATUS=implemented_on_fork_branch FEAS=small
  WHERE: /Users/nabil/Git/glue branch ape14-wcs-autolink (b16a3703, 016d9cf2): glue/plugins/wcs_autolinking/wcs_autolinking.py (+231/-61), tests/test_wcs_autolinking.py (+404)
  REC: Open the upstream PR now from the two commits (reference #2152 and #2152's 'wrapper that preserves units' use case, describe IRIS SJI gwcs + raster -TAB). No CHANGES.md edit needed: CHANGES.md is generated from GitHub release notes (.github/release.yml) - just label the PR. No doc change needed (doc/ only lists the API). Codestyle is ruff via pre-commit (neither installed locally; file compiles, no line >120) - rely on CI.
  DOCFIX: IRIS_GLUE_GAP_PLAN.md line 12 'active work is ... starting with Phase 3.1' is stale: the branch implements it fully with tests passing; remaining work is the PR and the glue-solar _GlueWCS fix. APE14_AUTOLINK_TODO.md sections 2/3 are all unchecked although every item is done (see sec2/sec3 rows). TODO cites 'audit §4a of IRIS_IDL_GLUE_CAPABILITY_AUDIT.md' - the audit has no §4a; the claim lives in the 'What the current Glue plugin does' bullet (line 129-131) and the comparison row 'SJI and raster temporal coordination' (line 182).

[P3.1-sec2] TODO section 2 'Fix plan' checkboxes vs branch diff
  STATUS=implemented_on_fork_branch FEAS=n/a
  WHERE: glue/plugins/wcs_autolinking/wcs_autolinking.py on ape14-wcs-autolink
  REC: Tick all five boxes. Consider (optional, glue-core) using the values path for every non-high-level pair, not only reordered ones: it removes the dependence on wrapper world_axis_object_classes coherence (the real #2152 problem) and would make raster<->raster work without the glue-solar fix.
  DOCFIX: All section-2 checkboxes are stale (unchecked but done).

[P3.1-sec3] TODO section 3 tests checkboxes
  STATUS=implemented_on_fork_branch FEAS=n/a
  WHERE: glue/plugins/wcs_autolinking/tests/test_wcs_autolinking.py lines 512-912 on ape14-wcs-autolink
  REC: Tick all boxes; reword the round-trip box to 'analytic forwards/backwards checks' rather than adding a redundant pixel_to_pixel comparison.
  DOCFIX: Section-3 checkboxes stale (unchecked but done).

[P3.1-sec4] TODO section 4 downstream (glue-solar): regression test, _GlueWCS coherence, delete Phase 1.4
  STATUS=partial FEAS=small
  WHERE: /Users/nabil/Git/glue-solar glue_solar/sources/loaders/iris.py:35-70 (_GlueWCS); glue_solar/tests (no autolink test); IRIS_GLUE_GAP_PLAN.md Phase 1.4
  REC: (1) Fix _GlueWCS now in glue-solar (override world_axis_object_classes + world_axis_object_components as above, plus a unit test that HighLevelWCSWrapper(_GlueWCS(raster_wcs)).pixel_to_world round-trips). (2) Add the glue-solar regression test (DataCollection([sji, raster]) -> exactly one WCSLink on HPC axes) marked skipif on glue version once the PR is released. (3) Drop Phase 1.4 from the gap plan now - it was never implemented and the branch makes it unnecessary.
  DOCFIX: Gap plan Phase 3.1 says the change makes discovery 'transform through pixel_to_world_values/world_to_pixel_values only' - the branch only does that for reordered types; same-order pairs still transform through high-level objects, which is why _GlueWCS coherence matters. 'This single change makes IRIS autolinking work' is true for SJI<->raster but raster<->raster additionally needs the glue-solar _GlueWCS fix.

[P3.1-sec5] TODO section 5 acceptance criteria
  STATUS=partial FEAS=trivial
  WHERE: ape14-wcs-autolink; no upstream PR yet
  REC: Open the PR; link #2152 and glue-solar #44; mention that a units-changing BaseWCSWrapper must keep world_axis_object_classes/components coherent (or adopt the values-path-for-all-low-level option).
  DOCFIX: Section-5 boxes 1-3 can be ticked.

  VERIFY: All five items stand; every decisive number the auditor reported reproduces (0 links + has_celestial AttributeError on glue 1.27.0; 1 WCSLink on the branch with raster(20,50)->SJI(88.612,20.662), exact round trip, HPC agreement; 31/1/1 and 38 passed; wrapped raster pair 0 links vs unwrapped 1 link; _GlueWCS object classes still in deg -> ValueError). Repos untouched; my worktree removed. Things the auditor missed or got slightly off: (1) Codestyle was not actually run — I ran the CI-pinned ruff 0.15.20 (clean) and current ruff 0.16.5 (2 new PLR0917 hits at wcs_autolinking.py:82 and :148 that main lacks; make `forwards`/`backwards` keyword-only before opening the PR to survive pre-commit.ci's monthly autoupdate). (2) No gluestate/clone test on the new low-level path; clone(link) with low-level coords fails with GlueSerializeError because glue-core only has @saver(astropy WCS) — pre-existing, not caused by the branch, but expect the question in review; glue-solar docs already say IRIS sessions cannot be restored. (3) glue-solar docs/user_guide/loading-iris-level-2-raster-and-sji-data.rst:57 hard-codes 'Glue does not currently autolink' and must change with the glue release. (4) The a

######## profile-wcs

[profile-wcs.C] Piece C - 'slice' collapse function in the profile viewer (glue-core)
  STATUS=implemented_on_fork_branch FEAS=small
  WHERE: glue:profile-wcs commits 175e7129 + 98d7bb7c; glue/viewers/profile/state.py (FUNCTIONS 'slice', `slices = DDCProperty`, `_set_default_slices` called from `_reference_data_changed`, `ProfileLayerState.slice_view`, `update_profile` bypasses compute_statistic via `data.get_data(attribute, view=view)` +
  REC: Open this as the first glue-core PR (2 commits, resolve the one-line python_export conflict when reordering). Add a clamp or IncompatibleDataException for out-of-range reference slices before opening. No changelog file needed (glue release notes are generated from .github/release.yml).
  DOCFIX: PROFILE_WCS_RESTART_TODO.md Piece C: all four checkboxes are still unchecked although every item is implemented and tested on glue:profile-wcs. IRIS_IDL_GLUE_CAPABILITY_AUDIT.md line 152 '1-D profiles along a chosen axis' and line 173 'Glue Image and 1-D Profile viewers plus subsets' describe released glue 1.27.0, where a profile is always a collapsed statistic - the single-pixel spectrum that iris_xdetector / iris_raster_browser show needs this 'slice' function (or a 1-pixel subset); the audit should say so.

[profile-wcs.A] Piece A - opt-in WCSAxes in the profile viewer (glue-core)
  STATUS=implemented_on_fork_branch FEAS=small
  WHERE: glue:profile-wcs commits f767d320 + c3bcd75b; glue/viewers/profile/viewer.py (`MatplotlibProfileMixin._set_wcs`, `update_x_ticklabel`/`update_y_ticklabel` overrides, `apply_roi` pixel branch, `_script_header`/`_script_footer`), glue/viewers/profile/state.py (`wcsaxes` class flag, `wcsaxes_active` pr
  REC: Open as the second glue-core PR after C, with the design rule and the pixel-coordinate ROI model stated in the description (a reviewer will ask). Fix `_GlueWCS.world_axis_units` for wavelength in glue-solar before recommending WCSAxes mode to IRIS users.
  DOCFIX: PROFILE_WCS_RESTART_TODO.md: Piece A checkboxes unchecked though done; 'Explicitly dead: the x_att -> x_att_pixel swaps in apply_roi' is contradicted by the branch (swap reinstated only when wcsaxes_active) - the TODO/PR text should record that as the decision, not as dead; 'make it a plain method, not a ... property getter' - implemented as a non-mutating property. IRIS_GLUE_GAP_PLAN.md: Phase 3 has no item for this work at all (only a pointer at line 14-16) and the 'Current status' paragraph says active work is 'starting with Phase 3.1' - stale: Pieces A-C are implemented with green tests on glue:profile-wcs / glue-qt:profile-wcs, no PRs opened.

[profile-wcs.B] Piece B - glue-qt slice sliders + WCSAxes wiring, gated on glue-core capabilities
  STATUS=implemented_on_fork_branch FEAS=small
  WHERE: glue-qt:profile-wcs commit 1133c0e9; glue_qt/viewers/common/slice_widget.py (`MultiSliceWidgetHelper`: y_att/z_att both hasattr-guarded, `_plot_atts` uses `x_att_pixel` when present, clears stale sliders when data removed, skips sync while len(slices) != ndim), glue_qt/viewers/profile/data_viewer.py
  REC: Rewrite the commit without glue_qt/_version.py, open the glue-qt PR after the glue-core C/A PRs exist and reference them; state in the description that the new tests are skipped against glue-core <= 1.27.0 by design. Consider splitting the profile_tools.py unit fix out - it is mergeable today.
  DOCFIX: PROFILE_WCS_RESTART_TODO.md Piece B: checkboxes unchecked though done; 'gated on Piece A being present in the installed glue-core (version floor)' - branch uses hasattr feature detection instead; 'One fix needed upstream' (y_att guard) understates the change - the helper was refactored (x_att_pixel support, stale-slider clearing, ndim-change guard).

[profile-wcs.98d7bb7c] What commit 98d7bb7c 'Translate slice views across linked datasets' does, and its relation to Phase 3.2
  STATUS=implemented_on_fork_branch FEAS=n/a
  WHERE: glue:profile-wcs 98d7bb7c; glue/viewers/profile/state.py `ProfileLayerState.slice_view(data, pix_cid)`; python_export.py; test_state.py::test_slice_function_linked / test_slice_function_out_of_bounds
  REC: Do not count this toward Phase 3.2. It is the per-layer correctness fix that lets two linked rasters share one profile viewer's slice point; the plan's 'synchronized slicing' (image viewer raster slider <-> SJI frame, time-nearest join) is a separate, unstarted glue-core/glue-qt feature.
  DOCFIX: IRIS_GLUE_GAP_PLAN.md 'Coordinated raster browser ... synchronized slicing upstream' (line 39) and Phase 3.2 (line 148-153) could be misread as advanced by this branch; add one sentence that profile-wcs only translates slices between layers inside one profile viewer.

[phase-3.2-sync-slicing] Phase 3.2 synchronized slicing across viewers (cross-viewer slider join + time-nearest join)
  STATUS=not_implemented FEAS=large
  WHERE: none: glue-core 1.27.0 ImageViewerState.slices / fork ProfileViewerState.slices are per-viewer state; no code on any fork branch or upstream links slice positions between viewers
  REC: Keep Phase 3.2 as the last upstream step as the plan already orders it. When designed, reuse the link-walking idiom from slice_view (`ref[other.pixel_component_ids[axis], point]`) as the translation primitive.
  DOCFIX: 

[upstream-prs-and-2248] Upstream PR readiness (separate PRs per piece) and fate of glue-viz/glue#2248
  STATUS=partial FEAS=small
  WHERE: glue:profile-wcs (pushed to origin/profile-wcs, no PR), glue-qt:profile-wcs (pushed, no PR); PR glue-viz/glue#2248 head nabobalis:1dprofile_wcs; origin/1dprofile_wcs still exists
  REC: Close #2248 now with a one-line comment pointing to the restart (it is a draft, conflicting, 4 years stale, and every salvageable idea is already captured per commit f767d320's message), then delete origin/1dprofile_wcs - that ticks the first 'Order and acceptance' box. Open PR 1 = Piece C (2 commits), PR 2 = Piece A stacked on it, PR 3 = glue-qt Piece B referencing both.
  DOCFIX: PROFILE_WCS_RESTART_TODO.md 'Order and acceptance': both boxes correctly still unchecked (branch not deleted, nothing merged) - but the header 'Keep origin/1dprofile_wcs untouched ... until the salvage below is copied out' is now satisfied and should be updated to 'delete'. IRIS_GLUE_GAP_PLAN.md line 12-16 should say the three pieces are implemented on fork branches awaiting PRs rather than implying the profile work has not started.

  VERIFY: Re-ran every decisive check with fresh worktrees (removed afterwards; ~/Git/glue on main, ~/Git/glue-qt on macos-integration, status unchanged). The auditor's core findings hold: glue:profile-wcs (4 commits) + glue-qt:profile-wcs (1 commit) implement every Piece A/B/C checkbox of PROFILE_WCS_RESTART_TODO.md (all still unchecked in the file); glue profile suite 57 passed/1 skipped; glue-qt profile+common with both forks 81/1; against installed glue-core 1.27.0 68 passed/14 skipped with the expected 'installed glue-core has no ...' reasons and zero errors (gating works); glue-qt image suite 69/2/1xfail; design rule = TODO's proposal (`_wcsaxes_with_unit`: real coords, world x_att, x_display_unit == native unit), with the apply_roi x_att_pixel swap reinstated only in WCSAxes mode; 98d7bb7c is intra-profile-viewer per-layer slice translation through links (`ref[data.pixel_component_ids[axis], point]`), not Phase 3.2 cross-viewer sync; E2E on the IRIS 2014 C II raster reproduced wcsaxes_active True, slice equality, ROI on 'Pixel Axis 2 [x]', unit override -> plain axes, wavelength ticks '0.00000013328' because _GlueWCS leaves em.wl in 'm', and the IndexError on out-of-range reference sl
   X upstream-prs-and-2248: Blocker (3) 'pre-commit/ruff not verifiable here' is wrong: ruff installs from pip (pip install --target <scratch> ruff==0.15.20, the repos' pinned pre-commit rev). With the repo config, glue:profile-wcs FAILS codestyle: 3 x RUF059 in new test code (glue/viewers/profile/tests/test_state.py:229, :257 from 98d7bb7c; tests/test_viewer.py:336 from the A commits). main is clean on the same files; the hook's `--fix` will not fix them (unsafe fix). glue-qt:profile-wcs passes ruff. So the glue-core PRs need those 3 lines changed (`_, y = ...`) before CI is green, on top of the reorder/_version.py points. Minor: the reordered C->A tree is byte-identical to the branch tip (empty diff), not 'differs by
   X notes.sji-inner-wcs: The auditor's side observation 'with irispy 0.8.1 the SJI dataset's inner WCS is an astropy WCS, not gWCS, contrary to the gap plan' is wrong - it was fooled by the class name. The gap plan's Phase 3.1 wording (SJI = gWCS, raster = astropy -TAB WCS) is correct.
   X notes.irispy-read-files-2021: Partly wrong: the failure is not tied to spectral_windows=['Mg II k 2796']. irispy 0.8.1 read_files fails on the copy under irispy/data/test/raster/iris_l2_20210905_..._raster_t000_r00000.fits with ANY window (None or Mg II k) - 'Expected 187 NUV source filename rows, found 1872' - while the identically named copy under data/test/sns/ loads fine (RasterCollection with 8 windows, and with ['Mg II k 2796']). glue-solar's irispy_test_files fixture must simply avoid the raster/ copy of the 2021 file.

######## sync-slicing

[P3.2-a] Translate a pixel slice index between linked datasets (primitive for slice sync)
  STATUS=available_upstream FEAS=trivial
  WHERE: glue-core 1.27 site-packages: glue/core/fixed_resolution_buffer.py:48 translate_pixel; glue/core/link_manager.py:347 is_convertible_to_single_pixel_cid, :420 pixel_cid_to_pixel_cid_matrix; idiom ref[other.pixel_component_ids[axis], point] (same as fork profile-wcs glue/viewers/profile/state.py:527)
  REC: Use it as-is. Do not plan upstream work for the translation primitive; the plan's 'largest upstream effort' framing for P3.2 is wrong on this half.
  DOCFIX: IRIS_GLUE_GAP_PLAN.md:148-153 implies slice joining 'through the data links' needs new glue-core machinery; the link-side translation already exists (translate_pixel / Data.__getitem__ with a pixel cid). IRIS_GLUE_GAP_PLAN.md:198 'largest upstream effort' is overstated.

[P3.2-b] Synchronized slicing across viewers (viewer-state level)
  STATUS=not_implemented FEAS=small
  WHERE: glue-core glue/viewers/image/state.py:90 (slices DDCProperty), :202 _update_syncing (colour/alpha 'global_sync' only, not slices); glue-qt glue_qt/viewers/common/slice_widget.py:26 (state->slider callback), glue_qt/viewers/common/data_slice_widget.py:81 (play QTimer inside SliceWidget; there is no '
  REC: Ship the glue-solar-only stopgap now (no upstream dependency): (1) in _cube_data add a datetime64 'Time' component to SJI cubes; (2) an action 'IRIS: link SJI and raster in time' that computes nearest indices from the two Time components and adds ComponentLink(pixel t <-> pixel exposure, using/inverse) plus the JoinLink from P3.2-c; (3) a ~25-line sync function that attaches a 'slices' callback to each ImageViewer state in app.viewers (glue_qt/app/application.py:1330) translating the source slice point into every other viewer's non-displayed axes via ref[cid, point], with one shared guard, keeping callback refs on the viewer. ~100-120 lines incl. a test on the irispy sns pair. Upstream design if ever wanted: glue-core `ViewerState.translate_slices(other_state)` + a small SliceSyncGroup in glue/viewers/common (mirrors fork slice_view), glue-qt 'Sync slices with other viewers' checkbox in glue_qt/viewers/image/options_widget.ui; medium (~200-300 lines + tests across two PRs).
  DOCFIX: IRIS_GLUE_GAP_PLAN.md:148 labels this 'glue-core state + glue-qt UI'; a complete SJI<->raster stopgap needs neither. IRIS_IDL_GLUE_CAPABILITY_AUDIT.md:182 'nearest-exposure selection' missing is still true for the shipped branch. Plan text 'stepping the raster scan updates the SJI frame' - for the 4D stack the SJI time axis maps to two stack axes (scan, step); two ComponentLinks from the same SJI pixel cid are needed, which the prototype loop handles per axis.

[P3.2-c] Time-nearest join primitive (extension of Data.join_on_key)
  STATUS=available_upstream FEAS=trivial
  WHERE: glue/core/data.py:871 join_on_key (stores _key_joins :1036-1037); glue/core/joins.py:35 get_mask_with_key_joins (np.isin exact match only); glue/core/link_helpers.py:502 JoinLink (Link Editor 'Join on ID', glue-qt dialogs/link_editor/state.py:223); link_manager.py:187 calls join_on_key on add_link a
  REC: Drop the upstream join_on_key extension (YAGNI). Precompute the nearest raster exposure index as an SJI component (and exposure_index on rasters) in the glue-solar link action and register a JoinLink so it shows and can be removed in the Link Editor. Fold this into the Phase 1.4 'Link IRIS datasets' helper.
  DOCFIX: IRIS_GLUE_GAP_PLAN.md:151-152 'time-nearest join primitive (extension of Data.join_on_key to nearest-value matching on a Time component)' - not needed; exact join on a nearest-index component does it with zero upstream work, and the SJI has no 'Time' component to match on today (its time is the 'Time (Utc)' world axis in seconds).

[P3.2-d] Fork branch glue:profile-wcs 98d7bb7c 'translate slice views across linked datasets' - reusable for viewer-to-viewer sync?
  STATUS=implemented_on_fork_branch FEAS=small
  WHERE: /Users/nabil/Git/glue profile-wcs c3bcd75b; commit 98d7bb7c: glue/viewers/profile/state.py:505 ProfileLayerState.slice_view (translation at :527 `ref[data.pixel_component_ids[axis], point]`, IncompatibleDataException at :529/:531), glue/viewers/profile/python_export.py; tests test_state.py:199 test_
  REC: Reuse the idiom, not the method: if/when the glue-core upstream sync helper is written, factor slice_view's translation loop into glue/viewers/common (e.g. translate_slice_point(ref, slices, target_data)) and call it from both ProfileLayerState.slice_view and the sync helper. For glue-solar's stopgap just copy the 6-line loop.
  DOCFIX: /Users/nabil/Git/glue/PROFILE_WCS_RESTART_TODO.md is stale: 0 of 14 checkboxes ticked although Piece C (175e7129 + 98d7bb7c), Piece A (f767d320, c3bcd75b) and glue-qt Piece B (1133c0e9) are implemented with green tests; it never mentions the cross-dataset slice translation / IncompatibleDataException behaviour added in 98d7bb7c (Piece C bullets at :129-141 describe only the raw slice view).

  VERIFY: All four items re-executed; auditor's file:line references, script outputs and test results reproduce exactly. One design-level correction (P3.2-b): the stopgap as recommended has no way to hook viewers opened after the action (no viewer-added hub message in glue-core; GlueApplication.new_data_viewer broadcasts nothing - verified by a later-created viewer staying unsynced), and the 4-D stack needs three links (two forward + one 2-input reverse), not two. Status/size judgements stand: P3.2 is a small glue-solar-only feature (Qt end-to-end verified with real sliders in scratchpad/vf_qt_sync.py), the join_on_key 'nearest' extension is unnecessary, and the plan's 'largest upstream effort' at IRIS_GLUE_GAP_PLAN.md:198 and 'glue-core state + glue-qt UI' at :148 are wrong. Auditor's environment note is now stale: ~/.glue/settings.cfg has unit_converter = \"default\" (mtime 08:19, a settings.cfg.bak sits in the scratchpad); `import glue` and the fork test suites pass with the real HOME. Missed by the auditor: closure ComponentLinks fail GlueSerializer with \"Object '__main__.<lambda>' not found\" (confirmed by run, consistent with its blocker note); join-key components broadcast to the ful
   X P3.2-b: Status 'not_implemented', feasibility 'small' and 'no glue-qt change needed' all hold (verified end-to-end in a real offscreen GlueApplication with two ImageViewers: dragging viewer A's Qt slider to 5 moves viewer B's state and slider to 15; B slider 100 -> A slider 33; both figures draw). Two gaps in the recommended stopgap design: (1) there is no viewer-creation hook - glue/core/message.py defines no viewer-added message and GlueApplication.new_data_viewer (glue_qt/app/application.py:1014) broadcasts nothing - so a 'sync function that attaches to app.viewers' only covers viewers open at action time; a viewer opened afterwards stayed at (0,0,0) while the hooked one moved to (31,0,0). The de

######## units-lines-blink

[P3.3] Velocity display units (wavelength <-> velocity via rest wavelength)
  STATUS=not_implemented FEAS=small
  WHERE: Machinery available upstream: glue-core 1.27 site-packages glue/core/units.py:9-70 (UnitConverter passes data+cid to helper; SimpleAstropyUnitConverter; find_unit_choices), glue/config.py:567-583,833 (unit_converter registry), :936-942 (settings.UNIT_CONVERTER + validator), glue/viewers/profile/stat
  REC: Ship in glue-solar (~15 lines + 1 test): subclass SimpleAstropyUnitConverter, read data.meta.rest_wavelength (or an override key set by an optional 'set rest wavelength' action), append 'km / s', convert with u.doppler_optical; install with unit_converter.members['default'] = cls at plugin setup; prune choices to m/nm/Angstrom/km/s. Optional 2-line glue-core PRs: x_axislabel with [unit] (viewer.py:22) and a warn-and-fallback in check_unit_converter. Drop the 'image axes' claim.
  DOCFIX: PLAN §3.3 'Velocity display units (glue-core). Register a spectral unit converter ... with glue's display-unit machinery so profile and image axes can show km/s. glue-solar contributes only the rest-wavelength UI affordance.' — wrong home: the registry and data/cid pass-through already exist in glue-core 1.27 and the converter runs from glue-solar today; 'image axes' is not covered by display units at all (only colour levels); rest wavelength needs no UI for the default since irispy already provides meta.rest_wavelength from TWAVE. PLAN table row 'Wavelength/velocity + line IDs | glue-core + glue-qt | Velocity display units in core' — should be glue-solar (+ optional cosmetic core PRs). AUDIT row 'Wavelength WCS only | Partial | No velocity conversion, rest-wavelength selection' — accurate for glue-solar today; rest wavelength selection is already resolved by irispy metadata.

[P3.4] Line-list overlay (labelled vertical lines from a table) in the Profile viewer
  STATUS=not_implemented FEAS=small
  WHERE: Nothing today: glue/viewers/profile (core) and glue_qt/viewers/profile have no annotation layer; only glue_qt/viewers/profile/mouse_mode.py:53,167 use axvline for ROI/navigation. Enabling hook available upstream: glue/config.py:734-771 layer_artist_maker registry, consulted in glue/viewers/common/vi
  REC: Implement in glue-solar (~50 lines + test): LineListArtist + LineListState(attribute) registered with layer_artist_maker for MatplotlibProfileMixin viewers, positions converted through UnitConverter so they track x_display_unit/km/s; ship an IRIS line list as a CSV/Data (C II 1334.53/1335.71, Si IV 1393.76/1402.77, O IV, Mg II k/h ...). Send the two one-line guards upstream (glue-core state.py:292, glue-qt data_viewer.py:19) so the monkeypatches can be dropped; a generic 'annotation layer' in glue-qt is unnecessary.
  DOCFIX: PLAN §3.4 'Line-list overlay (glue-qt). Vertical line annotations with labels in the Profile viewer, fed from a simple table.' — home is wrong/overstated: glue-core's existing layer_artist_maker lets glue-solar do this now; glue-qt/glue-core need at most two one-line guards. PLAN table row 'line-list overlay in qt' — same. AUDIT 'No ... line-identification overlay' — correct today.

[P3.5] Blink / mix of two image layers
  STATUS=partial FEAS=small
  WHERE: Mix = already available upstream: per-layer alpha slider glue_qt/viewers/image/layer_style_editor.py:18 (value_range 0-1) blended by glue/viewers/image/composite_array.py:170-185; RGB compositing = ImageViewerState.color_mode 'One color per layer' (glue/viewers/image/state.py:91-94,129). Playback/sp
  REC: Treat 'mix' as done (alpha slider + RGB mode) and delete it from the gap list. Ship blink as a ~25-line CheckableTool in glue-solar (QTimer toggling visible on the enabled layers, interval from a small spinbox), appended to ImageViewer.tools at plugin setup; upstream to glue-qt later if others want it. Copy jdaviz's click-to-advance semantics as an optional variant.
  DOCFIX: PLAN §3.5 'Blink/mix (glue-qt). Alternate or blend two image layers with adjustable rate/ratio — the one iris_ximovie behavior generic Glue lacks.' — blend with adjustable ratio already exists (per-layer alpha, RGB mode); only timed blink is missing and it needs no glue-qt change. PLAN table 'blink/mix in glue-qt' -> 'blink tool in glue-solar; mix exists'. AUDIT row 'SJI inspection ... No ... blink/mix' -> only blink is missing. PLAN §3.5 'Playback, speed and export of the current slice already exist' — confirmed correct (data_slice_widget.py:81-167).

  VERIFY: All three verdicts hold after re-execution: P3.3, P3.4 and P3.5 need no glue-core/glue-qt work — unit_converter (glue/core/units.py, config.py:567-583/936-942), layer_artist_maker (common/viewer.py:26-34,212,259) and viewer_tool registries in glue-core 1.27 + glue-qt main suffice, 'mix' already exists (per-layer alpha + 'One color per layer'), and the plan's Phase-3 homes for 3.3-3.5 are wrong. Reproduced: velocity converter via members['default'] override (-675.66 km/s, ROI round trip OK, label stays 'Wavelength'), the settings.UNIT_CONVERTER persistence trap (`import glue` KeyError at config.py:938 with a persisted non-default name — reproduced in a fake HOME), the line-list artist prototype, and a QTimer blink tool. Corrections: (a) P3.4 auditor's style-widget shim keyed on ProfileLayerArtist silently removes the style editor for the real QThreadedProfileLayerArtist layer (exact-class lookup, layer_artist_model.py:365-370) — key on QThreadedProfileLayerArtist; (b) the upstream .attribute guard is two lines (profile/state.py:266 and :292), not one; (c) profile/viewer.py x-label assignment is :23 not :22. Missed by auditor: 4-D stacks carry a plain-dict meta (stack_spectrograms.py
   X P3.4: Status (not implemented), feasibility (small, glue-solar via layer_artist_maker) and doc_corrections stand — I re-ran scratchpad/linelist_ok.py stage2 and got identical output. Two things in the evidence/recommendation are wrong: (1) the auditor's shim ProfileViewer._layer_style_widget_cls = {ProfileLayerArtist: ProfileLayerStyleEditor} silently DROPS the style editor for the real profile layer: glue_qt/core/layer_artist_model.py:365-370 does an exact-class dict lookup and the Qt artist class is QThreadedProfileLayerArtist (a subclass). Verified: with their key v._view.layout_style_widgets == [] ; keyed on QThreadedProfileLayerArtist it is ['QThreadedProfileLayerArtist']. 'layer list shows 2

######## session-serialization

[P3.6] Session serialization for IRIS datasets (overall) — 'done completely or not at all'
  STATUS=not_implemented FEAS=small
  WHERE: glue-solar glue_solar/sources/loaders/iris.py:35 (_GlueWCS, no __gluestate__); glue-core site-packages glue/core/state.py:655 (@saver(WCS) only), :744 (_save_style), :1023 (_save_data_5 meta filter)
  REC: Refute 'complete unit spanning glue-core': ~60 lines in glue-solar (_GlueWCS.__gluestate__/__setgluestate__ + meta sanitizer in _cube_data) make sessions work today; only the preferred_cmap JSON crash wants a 1-line glue-core fix (`getattr(cmap,'name',cmap)` in _save_style). Ship as Phase 1 glue-solar work with the three coordinate round-trip tests the plan asks for; drop the 'keep documenting as unsupported' clause once merged.
  DOCFIX: PLAN L52/L165-171: 'glue-core + glue-solar ... gWCS and APE-14 low-level WCS savers in glue-core (or glue-astronomy)' — no glue-core/glue-astronomy saver is needed; a __gluestate__ on glue-solar's _GlueWCS suffices (glue dispatches on __gluestate__ before MRO, state.py:380). 'Only as a complete unit' — the pieces are independent; SJI works with gwcs saver + cmap fix alone. AUDIT L141-144 / docs rst L64 'cannot currently be restored reliably' — understates: saving itself raises (three distinct exceptions), nothing is written. AUDIT: 'irispy metadata ... require serialization support' — SJIMeta round-trips today (148 keys, none dropped); only SGMeta's Quantity values crash, and Time/SkyCoord are already filtered silently by glue-core.

[P3.6-gwcs] gWCS (SJI) saver
  STATUS=available_upstream FEAS=trivial
  WHERE: asdf 5.3.1 + asdf-astropy 0.11 + gwcs 1.0.3 (already installed as irispy deps); prototype scratchpad/sess_proto_savers.py to_rec kind='asdf'
  REC: Serialize gwcs as a base64 asdf blob inside _GlueWCS.__gluestate__; no glue-core change.
  DOCFIX: PLAN L166 'gWCS ... savers in glue-core (or glue-astronomy)' — not needed; asdf is already a transitive dependency and round-trips irispy's SJI gwcs unchanged.

[P3.6-tab] -TAB (raster) saver persisting the lookup table
  STATUS=not_implemented FEAS=small
  WHERE: astropy 8.0.1 astropy/wcs/wcs.py:2874 to_fits (writes only det2im/distortion), :254 _load_tab_bintable (read-only callback); irispy/io/spectrograph.py:60-111 builds the WCS-TABLE synthetically; prototype sess_proto_savers.py tab_to_bytes
  REC: Implement the ~20-line IRIS-specific rebuild in glue-solar (mark ponytail: relies on irispy table layout); optionally open an astropy issue for -TAB in WCS.to_fits / Tabprm index exposure as the generic upgrade path.
  DOCFIX: PLAN L167 '-TAB saver that persists the lookup table (not just the header)' — correct in spirit, but astropy cannot export it; the table must be rebuilt from Tabprm.coord, and glue's existing @saver(WCS) header-string path raises rather than silently dropping the table.

[P3.6-compound] Compound-WCS (stack) saver
  STATUS=not_implemented FEAS=trivial
  WHERE: glue_solar/sources/loaders/stack_spectrograms.py:64-72 CompoundLowLevelWCS(target_wcs(-TAB), SlicedLowLevelWCS(2D LINEAR, [slice(None),0])); ndcube 2.4.1 compound_wcs.py:72 (_wcs tuple, .mapping.mapping)
  REC: Recursive to_rec/from_rec inside the same _GlueWCS saver; no ndcube change.
  DOCFIX: PLAN L168 'a compound-WCS saver' as glue-core work — it is 6 lines of glue-solar code once the -TAB and gwcs leaves exist.

[P3.6-meta] Metadata sanitizer
  STATUS=partial FEAS=trivial
  WHERE: glue-core state.py:1023-1038 _save_data_5 (filters meta by catching GlueSerializeError only); glue_solar/sources/loaders/iris.py:76 data.meta = cube.meta
  REC: One line in glue-solar `_cube_data` (keep scalar values, or move Quantity/Time/SkyCoord to components as needed) — or, better upstream, change `except GlueSerializeError` to `except Exception` in _save_data_5 (1 line in glue-core; no open issue found).
  DOCFIX: AUDIT L142 'irispy metadata ... require serialization support' — only the raster SGMeta's two Quantity values break saving; everything else is already handled by glue-core's filter.

[P3.6-cmap] Preferred-colormap sanitizer
  STATUS=not_implemented FEAS=trivial
  WHERE: glue-core state.py:744-756 (_save_style returns raw Colormap; _load_style removes 'preferred_cmap' so it is never restored); glue_solar/sources/loaders/iris.py:77 VisualAttributes(preferred_cmap='irissji1330' -> LinearSegmentedColormap)
  REC: Open a 1-line glue-core PR: `preferred_cmap: getattr(style.preferred_cmap, 'name', style.preferred_cmap)` in _save_style (loader already ignores it). Until released, glue-solar can register version-2 style saver/loader or drop preferred_cmap to a layer-state default when opening the viewer.
  DOCFIX: PLAN L168-169 'metadata and preferred-colormap sanitizers in glue-solar' — the cmap crash is a glue-core bug affecting any plugin that sets preferred_cmap to a Colormap object; the sanitizer belongs upstream. Note preferred_cmap is never restored by glue even after the fix (by design, state.py:752).

[P3.6-loadlog] Session size / re-load-from-file (LoadLog) for browser-loaded data
  STATUS=partial FEAS=small
  WHERE: glue-core data_factories/helpers.py:238-300 load_data attaches LoadLog; glue_solar/sources/iris.py:44 app.add_datasets(dialog.datasets) bypasses load_data; glue_solar/sources/iris.py:22 read_iris_file factory goes through it
  REC: Have the browser build datasets via load_data(path, factory=read_iris_file, **kwargs) per file so sessions store paths instead of arrays; otherwise document that browser-loaded sessions embed data.
  DOCFIX: Neither document mentions that glue sessions normally reference files (LoadLog) and that the browser path forfeits this; this matters more for usability than the WCS savers.

[P3.6-forks] Fork branches / TODOs vs session work
  STATUS=not_implemented FEAS=n/a
  WHERE: glue fork branches ape14-wcs-autolink, profile-wcs; glue-qt profile-wcs; APE14_AUTOLINK_TODO.md:80, PROFILE_WCS_RESTART_TODO.md:35, MACOS_INTEGRATION_TODO.md:139
  REC: No fork work to reuse; session serialization is untouched everywhere.
  DOCFIX: 

  VERIFY: All auditor findings reproduce except one evidence detail: on astropy 8.0.1 `to_header_string()` on the -TAB raster WCS succeeds (header with PS/PV keys); the 'HDUList is required' ValueError comes from reloading (`WCS(Header)`, i.e. glue's _load_wcs), so glue's stock WCS saver would save silently and fail on load — reverse the P3.6-tab doc_correction. Everything else confirmed by re-running: three independent save failures (_GlueWCS unserializable; SGMeta Quantity -> np.save TypeError; preferred_cmap Colormap -> JSON TypeError); the 55-line glue-solar-only prototype (asdf for gwcs, Tabprm-coord rebuild for -TAB, recursive Sliced/Compound record) round-trips SJI/raster/stack with p2w equal — I extended the -TAB check to the full pixel grid and sub-pixel offsets (max diff 5.6e-17); __gluestate__ method form works without any @saver registration; version-2 VisualAttributes saver/loader in glue-solar is a working workaround that even restores the cmap; pickle/asdf-astropy cannot carry a -TAB WCS (astropy has no -TAB writer, only the load callback); LoadLog path gives 23KB/255KB sessions and reload works, but coords are always read from the record so a coords saver is needed either way
   X P3.6-tab: Status/feasibility right, evidence wrong on the failure point. On astropy 8.0.1 `w.to_header_string()` does NOT raise: it returns a 2880-char header carrying the PS2_0/PS2_1/PS2_2/PS3_* -TAB keys (my run). The ValueError 'HDUList is required to retrieve -TAB coordinates and/or indices' is raised by WCS.__init__ on RELOAD, i.e. by glue's `_load_wcs` (state.py:660-663), not by `_save_wcs`. So the doc_correction should read: glue's built-in @saver(WCS) path saves the -TAB header silently (table dropped) and the session then fails to load — the opposite of 'raises rather than silently dropping'. Additional evidence: pickle.dumps/loads of the -TAB WCS fails the same way (astropy __reduce__ -> to_

######## macos-integration

[MACOS-1] Issue 1: app name shows 'python' (argv, setApplicationName/DisplayName, CFBundleName via pyobjc)
  STATUS=implemented_on_fork_branch FEAS=trivial
  WHERE: glue-qt branch macos-integration (PR glue-viz/glue-qt#68), commit d8a3f715: glue_qt/utils/app.py (_fix_mac_app_name, get_qapp), pyproject.toml dependency 'pyobjc-framework-Cocoa; sys_platform == "darwin"'
  REC: Keep the hard platform-conditional dep (already degrades gracefully via ImportError). Add a 3-line test asserting applicationName()/applicationDisplayName()=='glue' on the real qapp fixture. Open a follow-up on conda-forge/glue-qt-feedstock after merge. Mention the Linux/Windows title suffix in the PR body.
  DOCFIX: TODO l.53-55 'Automated test can only assert applicationName()/applicationDisplayName()' — such a test was never added. TODO l.46-49 dependency item is checked but omits the conda-forge recipe, which is how most mac glue users install.

[MACOS-2] Issue 2: fonts/icons too small (drop Darwin -2pt/-1pt offset, FONT_SIZE=-1 sentinel + migration, hardcoded-size sweep, Qt6 tab handling, platform toolbar icon size)
  STATUS=implemented_on_fork_branch FEAS=trivial
  WHERE: glue-qt macos-integration: glue_qt/utils/app.py (default_font_size, _font_size_is_set, fix_tab_widget_fontsize, update_global_font_size), glue_qt/app/preferences.py, application.py:381/445, edit_subset_mode_toolbar.py:32/73, data_collection_model.py:457, layer_artist_model.py:203, base_widget.py:34 
  REC: Merge as is; note in the PR body that the 1-pt reduction on Linux/Windows is also dropped (TODO acceptance flagged this). Optionally skip the migration+save when QT_QPA_PLATFORM is offscreen/minimal, or drop the migration entirely (a stale 13 on macOS is harmless and users can re-save). Decide explicitly whether the 0.75x slice-widget font stays; the checked TODO item says 'keep only if it still reads well' with no evidence it was looked at.
  DOCFIX: TODO l.88-91 says 'decide whether to keep the 1-point reduction elsewhere or zero both' as if open — the branch zeroes both. TODO l.76-80 (px/pt mixing in app.py:83-90 and preferences.py:95) is fixed by the branch (pt on Qt5, skipped on Qt6). TODO l.98-101 is checked although the slice-widget 0.75x decision was not made. TODO l.13 'app.py:56 creates the app as QApplication([''])' describes main, not the branch.

[MACOS-3] Unchecked TODO checkboxes: which are done, not done, or manual-only
  STATUS=partial FEAS=small
  WHERE: /Users/nabil/Git/glue-qt/MACOS_INTEGRATION_TODO.md l.50-55, 110-115, 119-126
  REC: Do one manual pass (open Preferences, Link Editor, data importers; check About/Quit/Cmd-Tab/Dock) and tick or drop the items; delete the TODO file when the PR merges. Mark l.110 as done now.
  DOCFIX: l.110-111 is phrased as a task but is a no-op; l.120 is already satisfied by the PR body screenshots.

[MACOS-4] The three follow-up commits ('remove tox all?!', 'try to fix some CI?!', 'maybe?!!!!') are unrelated CI hygiene
  STATUS=implemented_on_fork_branch FEAS=trivial
  WHERE: glue-qt a34a2180 (tox.ini), b596af69 (pyproject.toml), bd4dce6b (glue_qt/app/tests/test_plugin_manager.py, glue_qt/viewers/common/tests/test_data_viewer.py); also .gitignore glue_qt/_version.py in d8a3f715
  REC: Split a34a2180+b596af69+bd4dce6b(+.gitignore) into a 'Fix CI: drop pytest-flake8, nonexistent all extra, close leaked viewers in tests' PR, merge it first, then rebase #68 onto it; or at minimum squash #68 into one commit with a descriptive message so 'maybe?!!!!' does not land in history. Drop the unexplained _checkboxes tweak unless a failure justifies it.
  DOCFIX: The PR body does not mention the CI changes at all; the TODO file says 'Linux/Windows CI green' as an acceptance item without noting main was already red.

[MACOS-5] Merge readiness of PR glue-viz/glue-qt#68 (squash, changelog, pyobjc decision, reviewer questions)
  STATUS=implemented_on_fork_branch FEAS=small
  WHERE: https://github.com/glue-viz/glue-qt/pull/68 (head bd4dce6b, base main 9780eaf9 == upstream main as of today; MERGEABLE, no reviews, 0 comments, no labels)
  REC: Before pinging maintainers: squash (or split per MACOS-4), rewrite the body to list the Linux/Windows effects (font +1pt, 'Title — glue' suffix), move the Qt5/py<3.12 question to an issue, apply the 'enhancement' label, add the applicationName test. Keep pyobjc as a hard darwin-only dep unless upstream objects; it is the only way to fix the menu-bar name without an .app bundle.
  DOCFIX: None in the plan docs; TODO l.126 'Remove this TODO file when merged' still pending.

[MACOS-6] Relation to the IRIS gap plan / capability audit
  STATUS=non_goal FEAS=n/a
  WHERE: /Users/nabil/Git/glue-solar/IRIS_GLUE_GAP_PLAN.md l.16-17 (status paragraph only); not mentioned in IRIS_IDL_GLUE_CAPABILITY_AUDIT.md
  REC: Update the plan's status line to 'open as glue-viz/glue-qt#68 (code complete, CI green, awaiting review)' or drop the sentence; do not add it to any IRIS phase.
  DOCFIX: IRIS_GLUE_GAP_PLAN.md l.16-17 is stale: the work is no longer merely 'scoped' in a TODO, it is an open PR with code complete since 2026-08-29.

  VERIFY: Overall the auditor's picture holds: branch macos-integration (= glue-qt#68, head bd4dce6b on 9780eaf9) is code-complete, CI-green, independent of profile-wcs and of the IRIS plan; the three '?!' commits are unrelated CI repairs (pytest-flake8/pytest-9 PluginValidationError; leaked viewers breaking test_removed_subset — both reproduced from logs and in a scratch worktree of main; the _checkboxes tweak is a no-op; the tox 'all: all' removal is cosmetic). Refutations/additions: (1) On macOS the About/Hide/Quit labels and menu title come from CFBundleName (qcocoahelpers.mm qt_mac_applicationName), so setApplicationName/DisplayName contribute nothing on mac — pyobjc is the whole fix, conda-forge users get nothing, and TODO l.22-25 is wrong (auditor missed it). (2) The window-title suffix affects native top-level windows only, not MDI viewers; the main window 'Glue' becomes 'Glue — glue' on Linux/Windows because endsWith is case-sensitive — use displayName 'Glue' or drop it. (3) A useful headless test exists: assert CFBundleName=='glue' after get_qapp on Darwin. (4) IMPORTANT for the user: ~/.glue/settings.cfg currently holds font_size = 9.0 (mtime 08:28:39 JST, after the auditor's -1.0
   X MACOS-1: Status/feasibility are right; two evidence/doc errors. (1) Qt's macOS About/Hide/Quit labels come from CFBundleName, not applicationName/DisplayName: qtbase 6.8 src/plugins/platforms/cocoa/qcocoahelpers.mm:184-190 qt_mac_applicationName() reads CFBundleGetValueForInfoDictionaryKey(CFBundleGetMainBundle(),'CFBundleName') first. So the setApplicationName/DisplayName lines do nothing for the mac menu; the pyobjc mutation is the entire macOS fix (verified: after info['CFBundleName']='glue', CFBundleGetValueForInfoDictionaryKey returns 'glue'). TODO l.22-25 ('on macOS Qt uses the display name for the auto-generated About/Quit labels') is wrong and the auditor did not flag it; conda-forge users (n
   X MACOS-2: Code findings verified; the side-effect note is stale and its 'practical effect is nil' conclusion is wrong. ~/.glue/settings.cfg now reads font_size = 9.0 again (mtime Sep 2 08:28:39 JST, after the auditor's 08:19 -1.0 write): some later run of main-based glue_qt code outside pytest rewrote it (glue_qt/conftest.py:66-70 redirects CFG_DIR to a tempdir for pytest, but a bare get_qapp() on main writes font_size = 9.0 — reproduced in an isolated HOME with the main worktree). Consequence on this machine: in a real Cocoa session (default 13pt) the branch treats 9.0 as a user override (9 != 13, no migration) and renders glue at 9pt — the 'too small' symptom the PR set out to fix (main would give 7

######## doc-accuracy

[doc-1-audit-plugin-bullets] Audit 'What the current Glue plugin does' bullets + comparison-table cells vs branch code
  STATUS=implemented_glue_solar FEAS=trivial
  WHERE: glue-solar@d3a2bd3 (== PR #44 head): glue_solar/sources/iris.py:15-48, loaders/iris.py:27-115, loaders/scan.py:110-179, loaders/stack_spectrograms.py:42-79
  REC: Bullets and table cells are accurate in effect; fix the three overstatements quoted below. Optionally make _GlueWCS prefer _AXIS_NAMES over irispy's names so SJI and raster expose identical HPC component names (one-line change at loaders/iris.py:41), which also fixes the user-guide Linking instruction.
  DOCFIX: (a) Audit L122-124 'Gives physical names to wavelength and helioprojective world axes' -> only when irispy leaves the name empty (raster); SJI keeps irispy's 'Longitude'/'Latitude'/'Time (UTC)'. (b) Audit L137-138 'It preserves each scan's detector values, mask' and table L174 'The stack retains original values' -> masked pixels are replaced by NaN and the mask is rebuilt from isfinite; dtype is promoted to float32 and the array lives in a temp-file memmap. (c) Audit L141-144 session paragraph is correct, but for rasters the first failure is the Quantity-valued irispy metadata, not the -TAB WCS; keep 'metadata' first in the list. (d) Audit L129-131 / plan L142-144 mechanism: glue skips the datasets because glue-solar's _GlueWCS is low-level (BaseWCSWrapper), and would crash (uncaught AttributeError) on the raw gWCS/-TAB objects; current wording ('does not autolink irispy's SJI gWCS and r

[doc-2-plan-status-paragraph] Plan 'Current status (2026-08-29)' paragraph vs reality on 2026-09-02
  STATUS=implemented_on_fork_branch FEAS=trivial
  WHERE: IRIS_GLUE_GAP_PLAN.md:10-17; ~/Git/glue branches ape14-wcs-autolink (b16a3703, 016d9cf2), profile-wcs (175e7129..c3bcd75b); ~/Git/glue-qt profile-wcs (1133c0e9), macos-integration (= glue-viz/glue-qt#68)
  REC: Rewrite the status paragraph for 2026-09-02: #44 open/awaiting review (no reviewer requested), Phase 3.1 implemented on fork branch ape14-wcs-autolink (tests green, IRIS autolink verified) and awaiting an upstream PR; Pieces A/B/C of the profile-viewer TODO implemented on profile-wcs branches; macOS work is PR glue-qt#68. Tick the completed boxes in both TODO files (or note they are stale) and replace 'audit §4a' with a pointer to the audit's 'Data loading and representation' paragraph. Mention that the TODO files are untracked local files.
  DOCFIX: L10-11 'is in review with CI green' -> 'open and mergeable, CI green on d3a2bd3, no reviews or review requests yet'. L11-13 'active work is the glue-core items — starting with Phase 3.1 (APE-14 autolink)' -> Phase 3.1 is already implemented on ~/Git/glue branch ape14-wcs-autolink (2 commits, tests pass, IRIS SJI+raster autolink yields one HPC WCSLink) but has no upstream PR. L14-17 'Detailed working TODOs ... live in the glue fork' -> they are untracked files (not on any branch); both checklists are all-unchecked despite the branches existing; the profile-viewer work (PROFILE_WCS_RESTART_TODO.md, profile-wcs branches in glue and glue-qt) is referenced as active work but maps to no plan phase (closest: Phase 3.2). L142-144 'audit §4a' -> no such section exists.

[doc-3-cross-document-consistency] Consistency between the plan and the audit (coverage labels, homes vs phases, order of attack, upstream-library claims)
  STATUS=partial FEAS=trivial
  WHERE: IRIS_GLUE_GAP_PLAN.md:30-53, 57-91, 93-135, 190-199; IRIS_IDL_GLUE_CAPABILITY_AUDIT.md:165-186
  REC: Align the coverage labels with the audit ('Will not implement' for Level-3 and EIS), give the detector-view row either a phase item or move it to non-goals, drop Phase 1.4 outright (3.1 exists on a branch) and rewrite 'Suggested order of attack' to match the decided order (3.1 upstream PR -> 2.1 moments -> Phase 1 items -> 2.2 -> 3.2 -> remainder). Delete the 'add dask as optional extra' sentence. Rename the plan's moment outputs to the real keys (intensity/centroid/width/velocity/velocity_width).
  DOCFIX: Plan L51 'Level-3 FITS generation | Missing' -> 'Will not implement' (audit L185). Plan L53 'EIS ... | Out of scope' -> 'Will not implement' (audit L186). Plan L52 'audit §"unsupported"' -> 'audit, Data loading and representation, last paragraph'. Plan L4 'For every Partial/Missing row' -> also lists Will-not-implement rows and a non-table item. Plan L38 detector row has no matching phase; non-goal L184 says the opposite. Plan L194-195 Step 1 includes 1.4 which Step 2 deletes; and the status paragraph's order (3.1 first, then 2.1) contradicts Steps 1-3. Plan L124 'Requires dask — add as an optional fitting extra' -> dask is already a hard dependency via irispy-lmsal; no extra needed. Plan L102-103 '0th/1st/2nd moments' -> returned keys are intensity, centroid, width (+ velocity, velocity_width).

[doc-4-user-guide-rst] docs/user_guide/*.rst statements vs code and vs the two documents
  STATUS=partial FEAS=trivial
  WHERE: docs/user_guide/loading-iris-level-2-raster-and-sji-data.rst:25-46, 54-66; guide-to-glue-1dprofile-viewer-for-iris-data.rst:24-34; loading-aia-and-hmi.rst; docs/dev_guide/loader-customization.rst
  REC: Fix the Linking sentence to name the SJI components ('Longitude'/'Latitude') as well, or better normalise the names in _GlueWCS so both datasets say 'Helioprojective Longitude/Latitude' and the doc stays as is. Drop 'when an _SDO folder is present'. Plan to replace the Linking paragraph with a version-gated note once the ape14 change is released upstream.
  DOCFIX: loading-iris...rst L58-59 'manually pair ``Helioprojective Longitude`` and ``Helioprojective Latitude`` between datasets' -> SJI datasets expose ``Longitude``/``Latitude``; only raster datasets expose ``Helioprojective Longitude``/``Helioprojective Latitude``. L28 'when an ``_SDO`` folder is present' -> any ``aia_l2_*.fits`` cutout found under the folder. loading-aia-and-hmi.rst L43 'AMI' -> 'AIA' (cosmetic).

[doc-5-changelog-whatsnew] changelog/ fragments and docs/whatsnew vs the branch
  STATUS=implemented_glue_solar FEAS=trivial
  WHERE: changelog/44.feature.rst, changelog/44.bugfix.rst, changelog/42.breaking.rst, pyproject.toml:18-23, docs/whatsnew/changelog.rst
  REC: Add the new ndcube>=2.0 direct dependency to 42.breaking.rst (or 44.feature.rst). Everything else in the fragments is accurate; leaving the deleted trivial fragments out is a judgment call (the sst-module removal is the only one a user could notice).
  DOCFIX: No fragment states the new ``ndcube>=2.0`` dependency added in pyproject.toml on this branch; 42.breaking.rst lists glue-core/glue-qt/irispy floors only.

  VERIFY: All five findings hold; one (doc-3) overstates three sub-points. Independently re-executed every decisive check with my own scripts (sv2_probe*.py, sv2_ape14*.py in the scratchpad) and my own throwaway worktrees (removed afterwards; no repo modified). Confirmed: PR #44 OPEN/MERGEABLE, zero reviews or review requests, CI green on d3a2bd3; no upstream PR for ape14-wcs-autolink or profile-wcs; all three TODO files untracked, APE14/PROFILE checklists all-unchecked; 'audit §4a' points to nothing. Code facts confirmed: _GlueWCS is BaseLowLevelWCS-only so glue 1.27.0 skips it (0 links) and WCSLink crashes on has_celestial; raw irispy WCS objects crash wcs_autolink uncaught in either order (gwcs lacks has_celestial); SJI datasets expose 'Longitude'/'Latitude'/'Time (Utc)' not 'Helioprojective ...' (user-guide Linking instruction is unfollowable for SJI); sessions fail (SJI: _GlueWCS; raster: Quantity meta first, then _GlueWCS); stack replaces masked pixels with NaN in a float32 memmap. Things the auditor missed or under-verified: (1) the ape14 branch link is functionally correct, not just present -- SJI[x,y] <-> raster[slit, step] with wavelength/time correctly unlinked, world round-trip a
   X doc-3-cross-document-consistency: Three of the sub-claims are not defects in the plan. (1) 'Plan L38 detector row has no matching phase; non-goal L184 says the opposite' -- L38 is an 'optional full-detector loader variant' (data loading) while L184 rules out 'detector mosaic chrome' (widget layout); these are not opposites. Only 'no phase item for the L38 row' holds. (2) 'Step 1 includes 1.4 which Step 2 deletes' is not a contradiction: plan L80-81 explicitly designs 1.4 as a stopgap 'deleted once Phase 3's autolink work lands upstream'. Dropping 1.4 now is a reasonable judgement given the ape14 branch exists, but a glue-core release is not scheduled, so it is a choice, not an error. The genuine inconsistency is the status p

######## CRITIC
 **Scripts (all in the scratchpad, prefix `cc_`):** `cc_collapse.py`, `cc_region.py`, `cc_rewrap.py`, `cc_timelink.py`, `cc_forkmap.py` (ran against a throwaway worktree of glue `ape14-wcs-autolink`, removed afterwards), `cc_stackwcs.py`; plus re-runs of `v_qt.py`. Repos untouched (`git status`: only the pre-existing untracked md/_version files).

## 1. Rows/items not covered by any finding

- **Plan L38 / Audit L172 Detector view** ("full-detector loader variant", "detector-aware scales", "linked row/column plots"): no finding. Not checked by anyone; irispy 0.8.1 `read_files` is per-window only.
- **Plan L40, L186-188 / Audit L174 per-scan WCS in the 4-D stack** ("needs glue-core slice-dependent WCS support"): no finding tested the premise. Verified (`cc_stackwcs.py`): `gwcs` + `astropy.modeling.tabular.tabular_model(3)` over stacked -TAB lon/lat tables reproduces every scan's own -TAB coordinates exactly (dlon = 0.0 for scans 0-2, ~30 loader lines, forward). Catch: `gwcs.WCS.numerical_inverse`/`world_to_pixel_values` fail ("too many values to unpack") — a custom inverse model is needed for links/subsets. So the non-goal's premise is wrong: glue-core needs nothing; the blocker is a loader-side inverse model (medium).
- **Plan L41 / Audit L175 sit-and-stare chunking**: no finding. The irispy `sns` test raster IS sit-and-stare and already loads as `(187, 40, nwl)` with axis 0 = time (viewer-composition verifier); whisker preset gives the "solar-y vs time" layout; chunking = glue slices/subsets. Row is effectively covered-by-default; neither doc says so.
- **Plan L35 / Audit L169 OBS metadata**: non-goal, unaudited. `irispy.obsid.ObsID(3860258481)` prints description, SJI filters, FOV, exposure, binning, cadence, linelist (verified) — free improvement of the Partial row, not mentioned anywhere.
- **Audit L88-90, L70/72/73, L171 "snapshot or sequence export", "animation", "PostScript/JPEG output", "IRIS-specific export workflow"**: no finding; plan L163-164 claims only current-slice export exists. Verified: no movie/animation/ffmpeg/imageio export anywhere in glue-qt or glue-core (grep empty; `glue_qt/plugins` = dendro_viewer, tools). Sequence export is a real, unlisted gap.
- **Plan L110-112 "prefill wings from a Profile-viewer wavelength subset" / Audit L91 `xmoment_moment`**: unaudited. Interaction: on fork profile-wcs in WCSAxes mode `apply_roi` builds the subset on `x_att_pixel`, so the follow-up must accept pixel-index ranges, not only wavelength lo/hi. See also §3.1 (the Collapse tool already has this range UI).
- Audit L152/L173 "1-D profiles" only via profile-wcs; fine. Everything else in both tables/phases has a finding.

## 2. Cross-area contradictions (verified)

- **P1.4 fate.** ape14 `P3.1-sec4` ("drop Phase 1.4 now") and doc-3 auditor vs viewer-composition P1.4 auditor+verifier ("complementary; fork WCSLink is frame-0 geometry, ~250 px off after 4.8 h; keep it"). Viewer-composition is right (sv_reach: 14/187 raster steps reached by fork link vs 186/187 time-aware). Keep P1.4 as `link_hpc` by physical type; 3.1 adds the pixel link.
- **Time-aware SJI ROI via all-ones `axis_correlation_matrix`.** viewer-composition verifier recommends it (sv_timeaware.py, "viewer still draws"); slit-overlay verifier says it breaks glue-qt `ImageViewer.format_coord`. Re-ran `v_qt.py`: PATCH=0 → `−33" −400" (world)`; PATCH=1 → `OverflowError cannot convert float infinity to integer`. slit-overlay wins; the only safe form is the ~10-line wrap of `glue.core.coordinate_helpers.world2pixel_single_axis` (rebound in `component_link`) — a glue-core monkeypatch, so viewer-composition's "time-aware in 20 lines" is "small, hacky, belongs upstream".
- **P1.6-dep as prerequisite.** slit-overlay auditor: prerequisite for P1.3/P1.4. Its verifier + viewer-composition: not for lon/lat-only links (raster→SJI works today); only once a synthesised time link exists. Verifier right.
- **Moment-map linking after 3.1.** science-moments verifier: "pixel LinkSame stays required even after 3.1 (fork raises IncompatibleWCS for raster3D↔map2D and map↔map)". Root cause is `APE14-TODO-4.2`/`P3.1-sec4` (deg object classes). Verified on the fork with the coherent `FixedGlueWCS` (`cc_forkmap.py`): raster3D↔map2D autolink = 1 (slit/step ↔ x/y), map2D↔map2D = 1; stock `_GlueWCS` = 0/IncompatibleWCS. The glue-solar coherence fix makes P2.1-link automatic on 3.1; explicit LinkSame needed only on released 1.27.
- **Coherence-fix size.** viewer-composition "3 lines, `world_axis_object_classes` only" vs ape14 "~15 lines, also `world_axis_object_components` getters". The getters are used by `world_to_pixel` (`high_level_objects_to_values`); the 15-line version is what autolinked above. 3-line version fixes only `pixel_to_world`.
- **P3.2 stopgap vs P3.6 sessions.** sync-slicing's closure `ComponentLink(using=lambda)` is unserializable (verified by its verifier) while P3.6 says sessions are ~60 lines away. Resolution verified (`cc_timelink.py`): precompute nearest indices as int32 components on both datasets, identity `ComponentLink([sji.id['nearest_exposure']], ras_pix0)` + `JoinLink` on the same component; `sji[ras_pix0, (3,0,0)] → 5`, `GlueSerializer` round-trips (6.7 KB), translation works after reload. Caveat: `translate_pixel` refuses ("Dependency on non-pixel component") — the sync callback must use the `data[cid, view]` idiom the prototypes already used.
- **P2.1 UI hook.** science-moments recommends `layer_action` and "open an ImageViewer on velocity"; viewer-composition: `layer_action` callback gets `(layer, data_collection)` only. Confirmed (`UserAction._callback(layer, dc)`; `Session.application` is a weakref never passed; only `menubar_plugin(session, dc)` reaches the app, as `browse_iris` does). Choose: layer_action without auto-opening a viewer, or menubar_plugin + dataset picker.
- **Cube-handle prerequisite (plan L113-115, P2.1-prereq "needed") vs science-fitting ("not needed").** Verified for moments too (`cc_rewrap.py`): `SpectrogramCube(d[cid], d.coords._wcs, None, u.Unit('DN_IRIS_NUV') under add_enabled_units(DN_UNIT), d.meta, mask=d['… mask'])` → `calculate_moments` runs (5 maps, ingest OK). The prerequisite is unnecessary; zero loader changes for the moments PR (stacks: dict meta → TWAVE fallback).
- **Environment notes are all stale.** Five auditors reported `unit_converter = "iris-velocity"` breaking `import glue` (caused and repaired by the units-lines-blink auditor); the macOS verifier then flagged `font_size = 9.0`. Current `~/.glue/settings.cfg` (08:36): `font_size = -1.0`, `unit_converter = "default"`. Nothing to act on.
- **Fork readiness wording.** doc-2 says profile-wcs/ape14 are "tests green, awaiting PR"; profile-wcs verifier found 3 RUF059 lint failures, ape14 verifier 2 PLR0917 hits on current ruff, and glue-qt B carries a gitignored `_version.py`. "Green" is pytest-only; both need lint fixes before PRs.
- profile-wcs auditor's "SJI inner WCS is astropy, not gWCS" — refuted by its verifier; everyone else agrees on gwcs.

## 3. Capabilities all auditors missed

1. **glue-qt Profile viewer "Collapse" tab + `glue.core.aggregate.mom1/mom2` + `AggregateSlice`** (`glue_qt/viewers/profile/profile_tools.py:31-38, 302-331`; `glue/viewers/image/state.py:51`). Draw a wavelength range in the Profile viewer → the Image viewer displays Sum/Mean/Median/Min/Max/Moment 1/Moment 2 over it, live, zero plugin code. Verified (`cc_collapse.py`): `AggregateSlice(slice(10,40), 20, np.nansum)` on the Mg II k raster gives `(8,109) == nansum(data[:,:,10:40])`. Changes audit rows L178-179 from Missing to Partial (pixel-unit moments, no rest wavelength/velocity/masking), and it IS the `xmoment_moment` "define the line interactively" UI the plan defers at L110-112.
2. **`irispy.obsid.ObsID`** — see §1.
3. **gwcs + `tabular_model(3)` per-scan stack WCS** — see §1 (forward exact; inverse is the real work).
4. **Checked and rejected: glue-core `RegionData`** (shapely 2.1.2 installed; glue-core requires shapely≥2) for the FOV polygon/slit. Renders only when the target Data has `coords=None`; with the sunpy-map WCS Data it crashes (`data_region.py:219 → coordinate_helpers.py:42 AttributeError: 'tuple' object has no attribute 'shape'`) — glue-core bug. The PolygonalROI subset (network verifier) stays the answer.
5. **glue-core datetime-axis bug already hits shipped data.** Every raster/stack Data carries a datetime64 `Time` component; `glue/utils/matplotlib.py:449` uses epoch 0001-01-01 vs matplotlib 3.11's 1970-01-01, no `set_epoch` anywhere → any Scatter/Histogram of `Time` mislabels dates today, not just the GOES plot.
6. No sequence/movie export exists anywhere (§1) — worth a plan row, since `iris_ximovie` export was the audit's explicit item.

## 4. Hidden dependencies / unreleased upstream

- **Ships on released glue-core 1.27.0 + glue-qt main, no fork:** P1.1, P1.2 (whisker only), P1.4 (`link_hpc` by physical type), P1.5, P1.6 (subset from `SLTPX1IX`), P1.7, P2.1 (rewrap; one Data + 5 components + 2 pixel LinkSame; menubar hook), P2.2, P2.3a, P2.4 (subset FOV), P3.2 stopgap (component-based nearest link; needs an SJI `Time` component in `_cube_data`; no viewer-added hook → rescan viewers lazily), P3.3 (`unit_converter.members['default']` override + TWAVE fallback for stacks), P3.4 (style-widget dict keyed on `QThreadedProfileLayerArtist`), P3.5 blink, P3.6 (glue-solar savers + v2 `VisualAttributes` saver).
- **Fork-only (unreleased):** 3.1 autolink; profile `'slice'` + WCSAxes (glue profile-wcs); glue-qt sliders (B). Anything assuming a single-pixel spectrum (P1.3 iris_raster_browser-style profile, "linked row/column plots") depends on profile-wcs C being released; on 1.27 fall back to `function='mean'` and gate on `hasattr(ProfileViewerState, 'slices')`.
- **Chains:** P1.3 ← P1.4; SJI→raster ROI in P1.3 ← synthesised time link + world2pixel wrap (§2). P2.1-link on fork ← `_GlueWCS` coherence fix (§2) — do that fix in glue-solar now; it also unblocks raster↔raster and the AIA astropy-WCS path in P2.4 (catch `ValueError`, not only `IncompatibleWCS`). P3.1 PR ← keyword-only tweak (PLR0917); glue-solar regression test ← a glue release. profile-wcs PRs ← 3 RUF059 fixes, reorder C→A, drop `glue_qt/_version.py`.
- **`_GlueWCS.world_axis_units` wavelength m→Å** (profile-wcs.A recommendation) ripples into P3.3 native unit, P2.2 wavelength extraction, P3.6 saver tests, existing tests, P1.3 `x_display_unit`; decide once, before P2.x.
- **P3.4 ↔ P3.6:** `layer_artist_maker` layers are not restorable without their own saver; P3.6's "sessions work" needs a LineList saver or drop-on-save.
- **P3.2 ↔ P3.6:** closure links break sessions — use component-based links (§2, verified).
- **New deps:** P1.5 `sunpy[timeseries]` (h5netcdf); P2.3b fiasco+CHIANTI (de-scope); macOS #68 pyobjc (darwin-only; conda-forge follow-up). #68 is independent of everything else.
- **Plan "Suggested order" vs real chain:** the moments PR has no dependency at all (rewrap). The browser-experience chain is `_GlueWCS` coherence fix → `link_hpc` → quicklook / P2.4 / P1.6 fallback. 3.1 PR is independent but pays off beyond SJI↔raster only after the coherence fix.

## 5. Decided items vs findings

- **Moments-only first PR:** still right, now cheaper — no loader prerequisite (rewrap), no new deps, ~100-150 lines. Corrections: audit rows L178-179 → Partial (Collapse tool); "maps as datasets linked to the source" → one Data, five components, two pixel LinkSame (automatic on fork + coherence fix); tests need explicit `rest_wavelength` (all test windows exclude TWAVE); if it must open a viewer, hook = `menubar_plugin`. "Numeric-only wings" consistent; the deferred prefill can read the Collapse tool's `x_range` / `RangeSubsetState` (pixel indices in fork WCSAxes mode).
- **Non-goals:** Level-3 and EIS unchanged (nothing contradicts). OBS XML stays a non-goal, but `ObsID` gives cheap metadata. Per-scan WCS non-goal: premise wrong (glue-core not needed; loader gwcs forward-exact; inverse model is the blocker) — keep deferred, reword.
- **"Session serialization only as a complete unit" (plan L165-171):** refuted — pieces are independent, glue-solar-only ~60 lines, and the `maps.py:30` `preferred_cmap` crash already breaks every glue-solar session on main. Reverse the decision: fix in glue-solar now.
- **Guiding principle 3 ("viewer behaviour belongs upstream"):** contradicted for 3.2-3.6 — all shippable from glue-solar through public registries (`unit_converter`, `layer_artist_maker`, `viewer_tool`, `saver/loader`, `ImageViewer.tools`). Keep it as "upstream later", not a gate. Principle 1 ("thin glue") strains only where the fix is a private monkeypatch: P1.6-dep (`world2pixel_single_axis` wrap) and P3.4 (`_layer_style_widget_cls` dict).
- **"3.2 largest upstream effort" (plan L198):** refuted (small glue-solar).
- **Status paragraph (L10-17):** "starting with 3.1" stale (implemented on fork, lint pending); "first glue-solar work after #44 is moments" consistent; add that 3.1 needs the glue-solar coherence fix to link anything but SJI↔raster.
