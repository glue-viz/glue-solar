units-lines-blink.json:
  audit:
    items: [3]
      [0]:
        id: P3.3
        title: Velocity display units (wavelength <-> velocity via rest wavelength)
        status: not_implemented
        where: Machinery available upstream: glue-core 1.27 site-packages glue/core/units.py:9-70 (UnitConverter passes data+cid to helper; SimpleAstropyUnitConverter; find_unit_choices), glue/config.py:567-583,833 (unit_converter
            registry), :936-942 (settings.UNIT_CONVERTER + validator), glue/viewers/profile/state.py:40-41,271-284,482-487, glue-qt glue_qt/viewers/profile/options_widget.ui:226 (combosel_x_display_unit). Rest wavelength already on
            Data: glue_solar/sources/loaders/iris.py:76 (data.meta = cube.meta, an irispy SGMeta) + /Users/nabil/Git/irispy/irispy/meta.py:194-204 (SGMeta.rest_wavelength from TWAVE, read-only). Nothing in glue-solar or any fork
            branch implements it.
        evidence: RAN a 12-line converter offscreen against raster_data() on irispy test file iris_l2_20210905_001833_3620258102_raster_t000_r00000.fits: Wavelength world cid units='m'; data.meta.rest_wavelength = 133.571 nm (SGMeta);
            after registering a converter that adds ['km / s','m / s'] when meta.rest_wavelength is set and converts with u.doppler_optical(rest): ProfileViewerState.x_display_unit choices contain 'km / s'; setting it converts
            profile x from 1.3327e-07 m to -675.66 km/s, x_min/x_max follow. Second run: overriding unit_converter.members['default'] with a SimpleAstropyUnitConverter subclass gives the same result WITHOUT touching
            settings.UNIT_CONVERTER. Precedent: jdaviz registers @unit_converter('custom-jdaviz') UnitConverterWithSpectral reading data.meta and using u.spectral() equivalencies (fetched jdaviz/app.py). Open upstream request: glue-
            viz/glue#2246 'Enable Spectral Axis type conversion' (2021, no PR). Gotchas found by running: (1) settings.UNIT_CONVERTER='iris-velocity' got PERSISTED to ~/.glue/settings.cfg by glue-qt (save_settings() writes every
            setting: glue/_settings_helpers.py:8-27; triggers glue_qt/core/dialogs.py:44, app/preferences.py:170, utils/app.py:96) and then `import glue` itself failed with KeyError from check_unit_converter (glue/__init__.py:35
            load_settings runs before any plugin registers) — I reverted the file. (2) x-axis label stays 'Wavelength' with no unit: glue/viewers/profile/viewer.py:22 sets x_axislabel = x_att.label and ignores x_display_unit (y
            label does include units, :29). (3) default choice list for 'm' is ~130 prefixed units (GsolRad, ZAU, ...) — unusable combo. (4) image viewer has NO axis display units: glue/viewers/image/state.py:500
            attribute_display_unit is only for colour levels; axes are WCSAxes.
        feasibility: small
        blockers: None for the profile viewer. Do NOT install via settings.UNIT_CONVERTER (persisted -> breaks `import glue` when plugin loads later/is uninstalled); override unit_converter.members['default'] with a subclass, or first
            land a lenient validator upstream. Image-axis velocity is a different problem (WCS-level, e.g. a loader-side spectral wrapper) and not covered by display units. Interacts with glue fork profile-wcs branch
            (PROFILE_WCS_RESTART_TODO.md:44-52 says x_display_unit stays the owner of axis formatting) — compatible.
        recommendation: Ship in glue-solar (~15 lines + 1 test): subclass SimpleAstropyUnitConverter, read data.meta.rest_wavelength (or an override key set by an optional 'set rest wavelength' action), append 'km / s', convert with
            u.doppler_optical; install with unit_converter.members['default'] = cls at plugin setup; prune choices to m/nm/Angstrom/km/s. Optional 2-line glue-core PRs: x_axislabel with [unit] (viewer.py:22) and a warn-and-fallback
            in check_unit_converter. Drop the 'image axes' claim.
        doc_corrections: PLAN §3.3 'Velocity display units (glue-core). Register a spectral unit converter ... with glue's display-unit machinery so profile and image axes can show km/s. glue-solar contributes only the rest-wavelength UI
            affordance.' — wrong home: the registry and data/cid pass-through already exist in glue-core 1.27 and the converter runs from glue-solar today; 'image axes' is not covered by display units at all (only colour levels);
            rest wavelength needs no UI for the default since irispy already provides meta.rest_wavelength from TWAVE. PLAN table row 'Wavelength/velocity + line IDs | glue-core + glue-qt | Velocity display units in core' — should
            be glue-solar (+ optional cosmetic core PRs). AUDIT row 'Wavelength WCS only | Partial | No velocity conversion, rest-wavelength selection' — accurate for glue-solar today; rest wavelength selection is already resolved
            by irispy metadata.
      [1]:
        id: P3.4
        title: Line-list overlay (labelled vertical lines from a table) in the Profile viewer
        status: not_implemented
        where: Nothing today: glue/viewers/profile (core) and glue_qt/viewers/profile have no annotation layer; only glue_qt/viewers/profile/mouse_mode.py:53,167 use axvline for ROI/navigation. Enabling hook available upstream:
            glue/config.py:734-771 layer_artist_maker registry, consulted in glue/viewers/common/viewer.py:26-34 and :212 (add_data) / :259 (add_subset). Coupling that a custom artist must satisfy: glue/viewers/profile/state.py:292
            (_update_y_display_unit_choices assumes every layer_state has .attribute), glue_qt/viewers/matplotlib/data_viewer.py:59 (reads layer_artist.is_computing), glue_qt/viewers/profile/data_viewer.py:19
            (_layer_style_widget_cls is a single class; dict form with per-class skip handled at glue_qt/core/layer_artist_model.py:362-373). No IRIS line list ships in irispy (grep: only obsid.py matches '1334'). jdaviz line lists
            are bqplot-based (jdaviz/configs/specviz) — not reusable in matplotlib glue-qt. No fork branch touches this.
        evidence: RAN a prototype offscreen: a 1-D table Data(wavelength[m], name[str]) + a 35-line LineListArtist(MatplotlibLayerArtist) registered via @layer_artist_maker on the real IRIS raster in glue-qt ProfileViewer. First attempt
            hung on a modal 'Failed to add data: LayerState object has no attribute attribute' (traceback ends at profile/state.py:262 _layers_changed -> :292). With a LayerState subclass exposing `attribute`, `is_computing =
            False`, and ProfileViewer._layer_style_widget_cls made a dict: add_data -> True, layers ['QThreadedProfileLayerArtist','LineListArtist'], 6 mpl artists (3 axvline + 3 annotate); switching x_display_unit to Angstrom moves
            the line to 1334.53; with the P3.3 converter and 'km / s' the same line lands at -264.8 km/s; canvas draw OK; layer list shows 2 rows. Script: scratchpad/linelist_ok.py.
        feasibility: small
        blockers: Three coupling points need one-line shims (plugin-side) or one-line upstream guards: state.py:292 getattr(layer_state,'attribute',None); glue-qt data_viewer.py:19 dict-form _layer_style_widget_cls; is_computing default
            on LayerArtist. Sessions cannot restore the custom artist (sessions are unsupported anyway per plan §3.6). glue-solar must author the IRIS line table itself (not in irispy).
        recommendation: Implement in glue-solar (~50 lines + test): LineListArtist + LineListState(attribute) registered with layer_artist_maker for MatplotlibProfileMixin viewers, positions converted through UnitConverter so they track
            x_display_unit/km/s; ship an IRIS line list as a CSV/Data (C II 1334.53/1335.71, Si IV 1393.76/1402.77, O IV, Mg II k/h ...). Send the two one-line guards upstream (glue-core state.py:292, glue-qt data_viewer.py:19) so
            the monkeypatches can be dropped; a generic 'annotation layer' in glue-qt is unnecessary.
        doc_corrections: PLAN §3.4 'Line-list overlay (glue-qt). Vertical line annotations with labels in the Profile viewer, fed from a simple table.' — home is wrong/overstated: glue-core's existing layer_artist_maker lets glue-solar do this
            now; glue-qt/glue-core need at most two one-line guards. PLAN table row 'line-list overlay in qt' — same. AUDIT 'No ... line-identification overlay' — correct today.
      [2]:
        id: P3.5
        title: Blink / mix of two image layers
        status: partial
        where: Mix = already available upstream: per-layer alpha slider glue_qt/viewers/image/layer_style_editor.py:18 (value_range 0-1) blended by glue/viewers/image/composite_array.py:170-185; RGB compositing =
            ImageViewerState.color_mode 'One color per layer' (glue/viewers/image/state.py:91-94,129). Playback/speed already exist: glue_qt/viewers/common/data_slice_widget.py:81-167 (QTimer, back/stop/forward, speed). Blink = not
            implemented anywhere: grep of glue/viewers/image, glue_qt/viewers/image for blink returns nothing; gh code search 'blink' -> glue-viz/glue-jupyter 0 hits, glue-viz/glue-qt 0 hits; glue issue search 'blink' -> only closed
            #118 (2017, unrelated). Only ecosystem precedent: jdaviz jdaviz/configs/imviz/plugins/tools.py BlinkOnce(CheckableTool, tool_id 'jdaviz:blinkonce') — one-shot click/'b' advances viewer.blink_once(); no timer blink.
            Plugin-side tool injection pattern exists: glue/plugins/tools/__init__.py:13-14 (deepcopy ImageViewer.subtools and append); viewer_tool registry glue/config.py:612-628, CheckableTool glue/viewers/common/tool.py:56-72.
        evidence: RAN a 20-line @viewer_tool BlinkTool(CheckableTool) with a QTimer cycling layer.state.visible, appended via ImageViewer.tools = deepcopy(ImageViewer.tools)+['image:blink'], offscreen with two 20x20 Data layers: toolbar
            lists 'image:blink'; activate + step -> visible [False, True], next step [True, False]; deactivate restores. Same run: layers[1].state.alpha=0.5 -> composite layer alphas [0.8, 0.5] after draw; color_mode choices
            ['Colormaps','One color per layer'].
        feasibility: small
        blockers: Needs an icon asset and a rate control (a menu_actions() spinbox or fixed 2 Hz). Blinking two datasets on the same axes requires they be linked/reprojectable already (autolink item P3.1) — independent of the tool.
            Nothing in glue-qt needs to change.
        recommendation: Treat 'mix' as done (alpha slider + RGB mode) and delete it from the gap list. Ship blink as a ~25-line CheckableTool in glue-solar (QTimer toggling visible on the enabled layers, interval from a small spinbox), appended
            to ImageViewer.tools at plugin setup; upstream to glue-qt later if others want it. Copy jdaviz's click-to-advance semantics as an optional variant.
        doc_corrections: PLAN §3.5 'Blink/mix (glue-qt). Alternate or blend two image layers with adjustable rate/ratio — the one iris_ximovie behavior generic Glue lacks.' — blend with adjustable ratio already exists (per-layer alpha, RGB
            mode); only timed blink is missing and it needs no glue-qt change. PLAN table 'blink/mix in glue-qt' -> 'blink tool in glue-solar; mix exists'. AUDIT row 'SJI inspection ... No ... blink/mix' -> only blink is missing.
            PLAN §3.5 'Playback, speed and export of the current slice already exist' — confirmed correct (data_slice_widget.py:81-167).
    checks_run: [9]
      [0]:
        command: grep unit_converter/find_unit_choices/x_display_unit across glue-core 1.27 site-packages; cat glue/core/units.py; sed glue/viewers/profile/state.py & viewer.py; grep glue_qt/viewers/profile/options_widget.ui
        result: Registry + data/cid pass-through exist; x choices via find_unit_choices(state.py:280); x label ignores unit (viewer.py:22); Qt combo present (ui:226)
      [1]:
        command: python (offscreen): raster_data(irispy test raster) -> register @unit_converter('iris-velocity') using data.meta.rest_wavelength + u.doppler_optical; settings.UNIT_CONVERTER=...; glue-qt ProfileViewer
        result: meta is SGMeta with rest_wavelength=133.571 nm; 'km / s' in x_display_unit choices; profile x 1.3327e-07 m -> -675.66 km/s; x limits converted; label stays 'Wavelength'
      [2]:
        command: python -c 'from glue_qt.app import GlueApplication; GlueApplication()' after the above
        result: KeyError 'Unit converter iris-velocity is not defined' at glue/__init__.py:35 load_settings -> config.py:938; ~/.glue/settings.cfg had unit_converter="iris-velocity" (persisted by glue-qt save_settings). Reverted with
            sed to "default"; import glue OK afterwards; only that line differs from the backup
      [3]:
        command: python (offscreen, settings._save_to_disk=False): override unit_converter.members['default'] with SimpleAstropyUnitConverter subclass
        result: 'km / s' available, conversion works, settings.UNIT_CONVERTER still 'default'
      [4]:
        command: python (offscreen) scratchpad/linelist_ok.py stage2: layer_artist_maker LineListArtist + LayerState(attribute) + is_computing=False + dict _layer_style_widget_cls on real IRIS raster
        result: add_data True; 6 artists; line at 1334.53 Angstrom after unit switch; -264.8 km/s with velocity converter; draw OK. Without the shims: modal 'Failed to add data: LayerState has no attribute attribute' from
            profile/state.py:292
      [5]:
        command: python (offscreen): @viewer_tool BlinkTool(CheckableTool)+QTimer appended to ImageViewer.tools; alpha=0.5 on second layer
        result: toolbar has 'image:blink'; visibility alternates per step; composite alphas [0.8, 0.5]; color_mode choices ['Colormaps','One color per layer']
      [6]:
        command: grep -rn blink/alpha/compos/rgb in glue/viewers/image and glue_qt/viewers/image; grep annot/axvline in profile viewers; grep is_computing/_layer_style_widget_cls in glue_qt
        result: No blink; alpha compositing in composite_array.py:170-185; axvline only in profile mouse_mode.py; is_computing read at glue_qt/viewers/matplotlib/data_viewer.py:59; profile style editor is a single class
            (data_viewer.py:19)
      [7]:
        command: gh api search/code blink in glue-viz/glue-jupyter, glue-viz/glue-qt, BlinkOnce in spacetelescope/jdaviz; gh issue view glue-viz/glue#2246; WebFetch jdaviz app.py and imviz tools.py
        result: 0 / 0 / 5 hits; #2246 'Enable Spectral Axis type conversion' OPEN since 2021 with no implementation; jdaviz custom-jdaviz converter reads data.meta and uses u.spectral(); jdaviz BlinkOnce is one-shot click-to-advance, no
            timer
      [8]:
        command: grep rest_wavelength irispy; git diff --stat main...profile-wcs; grep display_unit in that diff; grep TODO files for unit/velocity/blink
        result: SGMeta.rest_wavelength read-only from TWAVE (meta.py:194-204); profile-wcs branch touches x_display_unit paths but keeps units as owner (PROFILE_WCS_RESTART_TODO.md:44-52); no TODO covers blink or line lists
    notes: Side effect and repair: my first experiment set settings.UNIT_CONVERTER and glue-qt persisted it to ~/.glue/settings.cfg, which made `import glue` fail; I restored unit_converter = \"default\" (backup at
        scratchpad/settings.cfg.bak) and verified import. This is itself the key finding for P3.3: never install a plugin converter via settings.UNIT_CONVERTER. Net verdict for all three items: none needs glue-core/glue-qt work
        to ship — P3.3 (~15 lines), P3.4 (~50 lines), P3.5 blink (~25 lines) are all doable in glue-solar on glue-core 1.27 + glue-qt main using existing registries (unit_converter, layer_artist_maker, viewer_tool); 'mix'
        already exists. Optional upstream follow-ups are one-to-two-line PRs (x-axis label unit, lenient UNIT_CONVERTER validator, getattr guard at profile/state.py:292, dict _layer_style_widget_cls in glue-qt profile
        data_viewer.py:19). Plan's 'Suggested order' can move 3.3-3.5 out of 'on demonstrated demand / upstream' into Phase 1. Prototype scripts: scratchpad/linelist_ok.py (line list + velocity), inline blink/converter scripts
        in this transcript. Worktrees scratchpad/wt-glue-pw and wt-glueqt-pw belong to another agent; untouched. No repo files modified.
  verify:
    verdicts: [3]
      [0]:
        id: P3.3
        agree: True
        correction: 
        evidence: Re-ran everything in a fake HOME (no ~/.glue writes). raster_data(irispy test raster) -> 8 window Datasets; Wavelength cid units='m'; data.meta is SGMeta, rest_wavelength=133.571 nm (irispy/meta.py:194-204). Default
            choice list for 'm' = 131 entries. Overriding unit_converter.members['default'] with a 9-line SimpleAstropyUnitConverter subclass (u.doppler_optical(meta.rest_wavelength)): 'km / s' in ProfileViewerState.x_display_unit
            choices (132), profile x 1.3327e-07 m -> -675.66 km/s, x_min/x_max -> -675.66/-576.60, to_native(-675.66 km/s) -> 1.3327e-07 m (ROI round trip OK, viewer.py:49, state.py:99), settings.UNIT_CONVERTER stays 'default',
            canvas draw OK, axes xlabel stays 'Wavelength' (viewer.py:23 — auditor cited :22, that is the if-line). Persistence trap reproduced: HOME with settings.cfg unit_converter="iris-velocity" -> `import glue` raises KeyError
            from config.py:938 via glue/__init__.py:35; glue-qt save_settings callers at dialogs.py:44, preferences.py:170, utils/app.py:96 confirmed. Registry/pass-through refs (units.py:9-70, config.py:567-583, :833, :936-942,
            state.py:40-41/271-284/482-487, options_widget.ui:226) all correct. gh: glue-viz/glue#2246 OPEN since 2021-11, 0 comments. Additions the auditor missed: (1) 4-D stack loader uses meta=dict(cube_sequence[0].meta)
            (stack_spectrograms.py:80) so stacks have NO .rest_wavelength attribute — converter must fall back to meta.get('TWAVE1') or stacks get no km/s; (2) stronger upstream precedent than jdaviz: glue-qt's own profile test
            registers a u.spectral() converter (glue_qt/viewers/profile/tests/test_data_viewer.py:340-352, test_unit_conversion) — the profile-viewer path is upstream-tested; lcviz also registers @unit_converter (gh code search).
            Status not_implemented / small / doc_corrections stand.
      [1]:
        id: P3.4
        agree: False
        correction: Status (not implemented), feasibility (small, glue-solar via layer_artist_maker) and doc_corrections stand — I re-ran scratchpad/linelist_ok.py stage2 and got identical output. Two things in the evidence/recommendation
            are wrong: (1) the auditor's shim ProfileViewer._layer_style_widget_cls = {ProfileLayerArtist: ProfileLayerStyleEditor} silently DROPS the style editor for the real profile layer:
            glue_qt/core/layer_artist_model.py:365-370 does an exact-class dict lookup and the Qt artist class is QThreadedProfileLayerArtist (a subclass). Verified: with their key v._view.layout_style_widgets == [] ; keyed on
            QThreadedProfileLayerArtist it is ['QThreadedProfileLayerArtist']. 'layer list shows 2 rows' did not catch this. The dict form (plugin monkeypatch or upstream change to glue_qt profile/data_viewer.py:19) must key on
            QThreadedProfileLayerArtist. (2) The upstream 'one-line guard at profile/state.py:292' is incomplete: state.py:266 (`id(layer_state.attribute)` in _layers_changed, run on EVERY layer-property change such as visible) also
            assumes .attribute, so an upstream fix is two guards; the plugin-side LineListState.attribute property covers both (verified visible toggle OK).
        evidence: Ran scratchpad/linelist_ok.py 1 offscreen: add_data True, layers ['QThreadedProfileLayerArtist','LineListArtist'], 6 mpl artists, line at 1334.53 after Angstrom switch, -264.84 km/s with the velocity converter, draw OK,
            2 rows. Extended run (scratchpad/ulb_ll2.py): layout_style_widgets [] with ProfileLayerArtist key vs ['QThreadedProfileLayerArtist'] with the subclass key; visible toggle on the line-list layer OK. Source:
            layer_artist_maker consulted at glue/viewers/common/viewer.py:26-34, :212, :259 (correct); is_computing defined only in glue_qt profile/histogram layer_artist.py and read for all layers at
            glue_qt/viewers/matplotlib/data_viewer.py:59 (correct); axvline only in glue_qt profile/mouse_mode.py:53,167 (correct); irispy has no line table — utils/constants.py only maps window names to detectors, obsid.py
            'linelist' is the OBS linelist number (correct). Dict-lookup line is :364 on glue-qt main, :365 on the macos-integration checkout the venv imports.
      [2]:
        id: P3.5
        agree: True
        correction: 
        evidence: Wrote and ran my own 20-line @viewer_tool BlinkTool(CheckableTool)+QTimer (scratchpad/ulb_blink.py) appended to ImageViewer.tools (plain list at glue_qt/viewers/image/data_viewer.py:42): toolbar has 'image:blink';
            activate -> visible [False, True]; step -> [True, False]; deactivate -> [True, True]; layers[1].state.alpha=0.5 -> composite alphas [0.8, 0.5]; color_mode choices ['Colormaps','One color per layer']
            (image/state.py:91-94,129). Alpha slider range at glue_qt image/layer_style_editor.py:18, compositing at composite_array.py:170-190, slice playback QTimer/speed at data_slice_widget.py:81-171 — all as cited. grep
            blink/alternate/blend in glue/viewers/image + glue_qt/viewers/image: only composite_array comments/tests. gh code search: blink -> glue-jupyter 0, glue-qt 0, glue 1 (non-feature); BlinkOnce -> jdaviz
            imviz/plugins/tools.py (5 hits). Precedent for tool injection glue/plugins/tools/__init__.py:12-14 correct. Minor: 'needs an icon asset' is not a blocker — an existing glue icon name (I used 'glue_move') works. Status
            partial (mix exists, blink missing), small, doc_corrections stand.
    summary: All three verdicts hold after re-execution: P3.3, P3.4 and P3.5 need no glue-core/glue-qt work — unit_converter (glue/core/units.py, config.py:567-583/936-942), layer_artist_maker (common/viewer.py:26-34,212,259) and
        viewer_tool registries in glue-core 1.27 + glue-qt main suffice, 'mix' already exists (per-layer alpha + 'One color per layer'), and the plan's Phase-3 homes for 3.3-3.5 are wrong. Reproduced: velocity converter via
        members['default'] override (-675.66 km/s, ROI round trip OK, label stays 'Wavelength'), the settings.UNIT_CONVERTER persistence trap (`import glue` KeyError at config.py:938 with a persisted non-default name —
        reproduced in a fake HOME), the line-list artist prototype, and a QTimer blink tool. Corrections: (a) P3.4 auditor's style-widget shim keyed on ProfileLayerArtist silently removes the style editor for the real
        QThreadedProfileLayerArtist layer (exact-class lookup, layer_artist_model.py:365-370) — key on QThreadedProfileLayerArtist; (b) the upstream .attribute guard is two lines (profile/state.py:266 and :292), not one; (c)
        profile/viewer.py x-label assignment is :23 not :22. Missed by auditor: 4-D stacks carry a plain-dict meta (stack_spectrograms.py:80) so a converter must fall back to meta.get('TWAVE1'), not just meta.rest_wavelength;
        glue-qt already ships a u.spectral() converter test (glue_qt/viewers/profile/tests/test_data_viewer.py:340-352) which is a stronger 'supported upstream' precedent than jdaviz. Environment note: ~/.glue/settings.cfg
        currently has font_size = 9.0 (default is -1.0), written 08:24 — consistent with the macos-integration branch's font-size code saving settings (utils/app.py:96) during another agent's run; the auditor's
        scratchpad/settings.cfg.bak already contains unit_converter=\"iris-velocity\" so it is not a clean pre-corruption backup. Sizes are realistic: converter ~10-15 lines (+TWAVE fallback), line list ~50 lines + a CSV, blink
        ~25 lines; all glue-solar. No repo files touched; scripts left in scratchpad (ulb_conv.py, ulb_ll2.py, ulb_blink.py, vhome-ulb/).