viewer-composition.json:
  audit:
    items: [4]
      [0]:
        id: P1.2
        title: Viewer presets: Whisker and Spectroheliogram Image Viewers
        status: not_implemented
        where: glue-solar iris-observation-browser: glue_solar/sources/iris.py:48 is the only viewer-creation call (app.new_data_viewer(ImageViewer, data=dialog.first_image)); no x_att/y_att/slices code anywhere (grep of glue_solar/
            and docs/ for whisker|spectroheliogram|x_att found nothing)
        evidence: Ran scratchpad/vc_proof.py offscreen on stock glue 1.27.0 + glue-qt 0.1.dev6892 with the real irispy sns pair (SJI_1400 shape (62,40,37); raster Si IV 1403 shape (187,40,29), numpy axes 0=raster step, 1=slit,
            2=wavelength). GlueApplication.new_data_viewer(viewer_class, data=None, state=None) returns the viewer; ImageViewerState has x_att/y_att (pixel cids), x_att_world/y_att_world (derived), slices (tuple, len ndim;
            _set_default_slices -> (0,)*ndim; entries for x/y axes ignored). Whisker = v.state.x_att,v.state.y_att = pix[2],pix[0]; v.state.slices=(0,20,0) -> x_att_world 'Wavelength', y_att_world 'Helioprojective Longitude', image
            shape (187,29), slider on slit axis. Spectroheliogram = x_att,y_att = pix[2],pix[1]; slices=(100,0,0) -> image (40,29). All viewers drew via figure.canvas.draw(). Default view of a 3-D raster window is ALREADY x=Pixel
            Axis 2 (wavelength), y=Pixel Axis 1 (slit), slider on axis 0 (raster step) == the spectroheliogram layout.
        feasibility: trivial
        blockers: None. UI hook: reuse @menubar_plugin (gets session.application) as iris.py already does; glue_qt.config.layer_action(label, callback, single=True, data=True) exists for a right-click per-dataset action but its callback
            only receives (selected_layers, data_collection), not the application, so opening a viewer from it needs a workaround — stick with menubar_plugin.
        recommendation: Ship only the Whisker preset (~10 lines: new_data_viewer + x_att/y_att + slices at the requested slit index, or just leave the slit slider at 0 and let the user drag it). Do not build a Spectroheliogram preset: it is the
            default Image Viewer layout for every raster window; document that instead. Skip a slit-position dialog in v1 (the slit slider appears automatically).
        doc_corrections: Plan P1.2 and audit rows 'Spectroheliogram panels'/'Whisker plot' present both as needing a preset; the spectroheliogram view is what glue opens by default for a raster window (x=wavelength, y=slit, slider=raster
            position), so only the whisker needs code. Plan's 'a few lines each' is confirmed.
      [1]:
        id: P1.3
        title: Quicklook layout helper (load raster+SJI, HPC links, SJI + raster-map + Profile viewers)
        status: not_implemented
        where: glue-solar iris-observation-browser: glue_solar/sources/iris.py:31-48 browse_iris loads via QtIRISImporter (dialog.datasets, dialog.first_image) and opens one ImageViewer; no links, no map viewer, no profile viewer
        evidence: vc_proof.py: after dc.add_link(LinkSame pairs) the trio opens in one call each: app.new_data_viewer(ImageViewer, data=sji); map viewer with x_att,y_att = pix[0],pix[1], slices=(0,0,14) -> axes 'Helioprojective Longitude'
            x 'Helioprojective Latitude'; app.new_data_viewer(ProfileViewer, data=raster) with state.x_att = pix[2] (ProfileViewerState.x_att accepts pixel or world cid; x_att_pixel derived). A subset created on the SJI's Longitude
            cid appeared as an ImageSubsetLayerArtist in the raster map viewer (111360/216920 raster px). Profile viewer default state.function == 'maximum' (a quicklook spectrum wants 'mean', or the glue fork profile-wcs 'slice'
            collapse once it lands). Propagation is asymmetric, see P1.4: an ROI drawn on the SJI Image Viewer does NOT reach the raster with world links; drawn on the raster map it does reach the SJI.
        feasibility: small
        blockers: Depends on P1.4 helper for the links. 'True' coupled browsing (cursor/slice sync, nearest exposure) is Phase 3.2; nothing in glue 1.27 or the fork branches provides slice-to-slice synchronization across datasets except
            the glue fork profile-wcs commit 98d7bb7c (translate slice views across linked datasets), which is not what P1.3 assumes.
        recommendation: Extend browse_iris (not a new action): after app.add_datasets, call the P1.4 link function on the loaded SJI/raster datasets, then open the three viewers (~25 lines total) with profile function set to 'mean'. State
            honestly in the docs that SJI-drawn ROIs will not propagate to the raster until Phase 3.1/3.2; raster-drawn ones and world-coordinate subsets do.
        doc_corrections: Plan P1.3 'the honest subset of iris_raster_browser that Glue's linking already supports' overstates it: with the HPC world links, glue cannot invert the SJI gWCS (lon/lat depend on the time pixel:
            axis_correlation_matrix rows all ones), so image-viewer ROIs on the SJI raise IncompatibleAttribute on the raster; only raster->SJI ROI propagation and world-coordinate subsets work.
      [2]:
        id: P1.4
        title: Explicit link helper (programmatic HPC Longitude/Latitude LinkSame)
        status: not_implemented
        where: glue-solar iris-observation-browser: nothing (grep LinkSame|ComponentLink|add_link in glue_solar/ -> no hits); docs/user_guide/loading-iris-level-2-raster-and-sji-data.rst:57-59 tells users to pair the components by hand
        evidence: vc_proof.py on stock glue 1.27: LinkSame(cid1, cid2) is a MultiLink; DataCollection.add_link(links) accepts a ComponentLink, an iterable, or a LinkCollection; 2 LinkSame pairs -> 14 dc.links, and raster[sji_lon_cid] ==
            raster[raster_lon_cid], sji[raster_lon_cid] == sji[sji_lon_cid]. World cid LABELS are NOT stable across the pair: SJI (irispy gWCS carries axis names, which win in _GlueWCS.world_axis_names at loaders/iris.py:39-43)
            exposes 'Longitude','Latitude','Time (Utc)'; raster (-TAB FITS WCS, empty names -> _AXIS_NAMES) exposes 'Helioprojective Longitude','Helioprojective Latitude','Wavelength'. Matching must use
            data.coords.world_axis_physical_types (reversed to numpy order) zipped with data.world_component_ids — 4 lines. vc_roi.py results: LinkSame: world-inequality subset SJI->raster OK (111360/216920), raster pixel-ROI->SJI
            OK (19380/91760), SJI pixel-ROI (what the Image Viewer draws) -> raster IncompatibleAttribute. Fork ape14-wcs-autolink WCSLink: SJI pixel-ROI->raster OK (1740/216920), world subset -> IncompatibleAttribute, raster
            ROI->SJI 0 px. vc_acc.py: fork link output equals the world-value route at SJI time index 0 exactly (SJI px [21.1,88.6,225.8],[5.9,20.7,35.3]); vc_tab.py: SJI px(10,10) lon is -54.7 arcsec at t=0 but -12.5 at t=61, so a
            frame-0 pixel link is off by tens of arcsec for later SJI frames (fork's own kept_numpy_axes docstring: 'the link is then exact at the first exposure'). Stock glue wcs_autolink on the same pair: 0 links (audit §4a
            confirmed).
        feasibility: trivial
        blockers: None for the helper itself. P1.4 does NOT become moot when ape14-wcs-autolink lands: WCSLink is a pixel-to-pixel link exact only at SJI exposure 0 and gives SJI-ROI->raster; LinkSame on world cids gives world subsets and
            raster-ROI->SJI, per-frame correct, but cannot invert the SJI. They are complementary; neither is time-aware (that is Phase 3.2).
        recommendation: Write one ~10-line function link_hpc(datasets) that pairs world cids by physical type with LinkSame, call it from browse_iris automatically when both an SJI and a raster are loaded, and also expose it as a menubar action
            for File->Open loads. Keep it after Phase 3.1 lands; change the plan text from 'deleted once autolink lands' to 'kept; autolink adds the pixel link'.
        doc_corrections: (1) docs/user_guide/loading-iris-level-2-raster-and-sji-data.rst:57-59 'manually pair Helioprojective Longitude and Helioprojective Latitude between datasets' — the SJI side is labelled 'Longitude'/'Latitude' in the link
            editor. (2) Plan P1.4 'deleted once Phase 3's autolink work lands upstream', plan P3.1 'This single change makes IRIS autolinking work and removes Phase 1 item 4', suggested-order row 2 'deletes Phase 1.4', and
            APE14_AUTOLINK_TODO.md §4 bullet 3 'Delete explicit link helper ... once the released glue autolinks IRIS data' are all wrong: the two link kinds cover different subset directions. (3) Plan/audit call the SJI WCS 'gWCS'
            correctly (type module gwcs.wcs), fine.
      [3]:
        id: APE14-TODO-4.2
        title: _GlueWCS world_axis_object_classes coherence after arcsec conversion (APE14_AUTOLINK_TODO §4 bullet 2)
        status: partial
        where: glue-solar glue_solar/sources/loaders/iris.py:35-69 (_GlueWCS overrides names/units/values only; world_axis_object_classes inherited from the wrapped WCS)
        evidence: vc_acc.py on the fork worktree: _GlueWCS(SJI).world_axis_object_classes celestial unit = (arcsec, arcsec) (gWCS frame is natively arcsec, coherent); _GlueWCS(raster).world_axis_object_classes celestial unit = (deg, deg)
            while _GlueWCS.pixel_to_world_values returns arcsec — incoherent by 3600x for any high-level (SkyCoord / pixel_to_pixel) consumer. The fork's IRIS link is still exact because SJI sliced types are (lon,lat) and raster's
            are (lat,lon), which routes through permuted_values_functions (world values, no SkyCoord). Fork branch tests: 31 passed, 1 xfailed, 1 xpassed.
        feasibility: trivial
        blockers: None
        recommendation: In _GlueWCS override world_axis_object_classes to rewrite the celestial 'unit' kwarg to arcsec (3 lines), with a test that HighLevelWCSWrapper(_GlueWCS(raster)).pixel_to_world returns a SkyCoord in arcsec matching the
            low-level values. Do it in the glue-solar PR that adds link_hpc, independent of the upstream autolink PR.
        doc_corrections: APE14_AUTOLINK_TODO.md §4 bullet 2 is still open and the answer is 'not coherent for the raster'; the plan does not mention it.
    checks_run: [8]
      [0]:
        command: grep -rn -i 'whisker|spectroheliogram|quicklook|LinkSame|add_link|new_data_viewer|x_att|ComponentLink' glue_solar docs
        result: Only glue_solar/sources/iris.py:48 new_data_viewer(ImageViewer, data=first_image); nothing else
      [1]:
        command: python -c 'inspect signatures: GlueApplication.new_data_viewer, ImageViewerState x_att/y_att/slices, LinkSame, ComponentLink, DataCollection.add_link, ProfileViewerState'
        result: new_data_viewer(viewer_class, data=None, state=None); slices tuple len ndim; LinkSame(cid1, cid2) MultiLink; add_link(links: ComponentLink|iterable|LinkCollection); Profile x_att accepts pixel or world cid
      [2]:
        command: env HOME=<scratch>/home-vc QT_QPA_PLATFORM=offscreen MPLBACKEND=agg .venv/bin/python scratchpad/vc_proof.py (stock glue 1.27.0)
        result: OK: 2 LinkSame links by physical type; SJI world subset -> raster 111360/216920 px; whisker/spectroheliogram/map/profile viewers opened with expected x/y world atts and slices; default 3-D view == spectroheliogram; all 5
            viewers drew
      [3]:
        command: pytest glue/plugins/wcs_autolinking in detached worktree of glue ape14-wcs-autolink (016d9cf2) with PYTHONPATH=worktree
        result: 31 passed, 1 xfailed, 1 xpassed
      [4]:
        command: vc_autolink.py / vc_roi.py autolink on fork vs stock glue with real irispy sns SJI_1400 + raster Si IV 1403
        result: stock: wcs_autolink -> 0 links. fork: 1 WCSLink SJI (x,y) <-> raster (slit,step); SJI pixel-ROI->raster OK 1740 px; world subset -> IncompatibleAttribute; raster ROI->SJI 0 px
      [5]:
        command: vc_roi.py linksame (stock glue)
        result: world subset SJI->raster OK; raster pixel-ROI->SJI OK 19380/91760; SJI pixel-ROI->raster IncompatibleAttribute
      [6]:
        command: vc_acc.py / vc_drift.py / vc_tab.py
        result: fork link == world-value route at SJI t=0 exactly; SJI px(10,10) lon -54.7 arcsec at t=0 vs -12.5 at t=61 (time-dependent pointing); raster _GlueWCS object classes say deg while values are arcsec; -TAB inverse round-
            trips in-domain, NaN out of domain
      [7]:
        command: git worktree remove --force <scratch>/wt-ape14-vc; git status in glue and glue-solar
        result: My worktree removed; both repos unchanged (only the pre-existing untracked md files)
    notes: Side effects observed, not mine: ~/.glue/settings.cfg was rewritten at 08:10 today with unit_converter = \"iris-velocity\" (no such converter is registered in glue-solar, glue, or glue-qt sources); this makes `import
        glue` raise KeyError 'Unit converter iris-velocity is not defined' in the user's real environment until that line is removed or the converter is registered — presumably a sibling agent's GlueApplication run persisted an
        in-process registration. I did not touch the file; all my runs used HOME=<scratchpad>/home-vc. Two sibling worktrees remain in the scratchpad (wt-ape14 on ape14-wcs-autolink, wt-glue-pw on profile-wcs); I left them
        alone. Proof scripts: scratchpad/vc_proof.py, vc_roi.py, vc_acc.py, vc_drift.py, vc_tab.py. Overall verdict for the three items: all three are small glue-solar-only work on stock glue 1.27 (roughly 45 lines total
        including a whisker preset, link_hpc by physical type, and extending browse_iris); the plan's only real errors are (a) spectroheliogram preset is the default view, (b) P1.4 is not obsoleted by APE-14 autolink, (c) the
        docs name SJI components that do not exist ('Helioprojective Longitude' vs actual 'Longitude'), and (d) neither link kind is time-aware, so 'coordinated browsing' stays Phase 3.2.
  verify:
    verdicts: [4]
      [0]:
        id: P1.2
        agree: True
        correction: 
        evidence: Reproduced vc_proof.py on stock glue 1.27.0 + editable glue-qt (macos-integration; `git diff main...macos-integration -- glue_qt/app/application.py` has no new_data_viewer hunk, so the API claim holds for upstream glue-
            qt too): SJI (62,40,37), raster (187,40,29); whisker x_att=Pixel Axis 2, y_att=Pixel Axis 0, slices (0,20,0), x_att_world Wavelength; default 3-D viewer x=Pixel Axis 2, y=Pixel Axis 1, slices (0,0,0) == spectroheliogram;
            all viewers drew. grep of glue_solar/ and docs/*.rst for whisker|spectroheliogram|x_att|LinkSame|add_link finds only glue_solar/sources/iris.py:48. Extra check the auditor did not run (scratchpad/sv_slider.py): the
            MultiSliceWidgetHelper slider follows state.slices (default slider 'Helioprojective Longitude' at 0 -> after whisker preset a single 'Helioprojective Latitude' slider at 20), and glue/viewers/image/state.py:336-349
            confirms slice entries on the x/y axes are ignored. Nuance: the irispy 'sns' raster is a real sit-and-stare (OBS_DESC 'Medium sit-and-stare 0.3x60 1s'); its axis 0 is time, and the whisker's y label is 'Helioprojective
            Longitude' (slit position drifting 42 arcsec with rotation tracking) because the loader stores Time as a component, not a coordinate — so the plan's 'wavelength x raster/time' gets a spatial axis label in the time case.
            layer_action callback receives (layer, data_collection) only (glue_qt/app/layer_tree_widget.py UserAction._do_action) — agree, menubar_plugin is the hook.
      [1]:
        id: P1.3
        agree: False
        correction: Status (not_implemented), size (small) and the extend-browse_iris recommendation stand; two things are wrong. (1) 'glue cannot invert the SJI gWCS (lon/lat depend on the time pixel)' and 'SJI-drawn ROIs will not
            propagate to the raster until Phase 3.1/3.2' are wrong: irispy's gWCS inverse is time-aware (world_to_pixel_values(lon61,lat61,t61) -> (10,10,61)); the block is on glue's side — glue/core/coordinate_helpers.py:163-175
            dependent_axes reads the *forward* axis_correlation_matrix (time row [0,0,1]) so the world->pixel link for SJI x,y never receives world time and CoordinateComponentLink zero-fills it (frame-0 geometry), and the raster
            has no SJI time cid at all (hence IncompatibleAttribute). On stock glue 1.27, LinkSame(lon,lat) + one ComponentLink(raster.id['Time'] -> SJI 'Time (Utc)' world cid, datetime64 -> seconds since the gWCS reference) + a
            _GlueWCS subclass for the SJI returning an all-ones axis_correlation_matrix gives a TIME-AWARE SJI-ROI -> raster: whole-frame SJI ROI reaches 186/187 raster steps (fork WCSLink: 14/187), derived SJI (x,y,t) at raster
            step 93 = (23.3,20.7,30.5) == direct inverse, SJI Image Viewer still draws with 'Longitude'/'Latitude' axes (scratchpad/sv_timeaware.py, ~20 lines). So the honest quicklook trio can propagate ROIs in both directions
            today; only synchronized slider stepping stays Phase 3.2. (2) ProfileViewer default x_att for the raster is 'Helioprojective Longitude' (axis 0, x_att_pixel Pixel Axis 0), not wavelength — the helper must set state.x_att
            = the 'Wavelength' world cid (accepted directly; x_att_pixel becomes Pixel Axis 2) and can set x_display_unit='Angstrom' (verified: 1398.6-1399.3 A) and function='mean'.
        evidence: scratchpad/sv_timelink.py linksame+time (stock glue): world subset OK 111360, SJI pixel ROI -> raster OK 1740 (was IncompatibleAttribute without the time link), raster ROI -> SJI OK 19380 — but derived SJI x at step 93 =
            147.6 (= frame-0 value) because dependent_axes zero-fills time. scratchpad/sv_timeaware.py (all-ones matrix): step 93 -> (23.3,20.7,30.5) = S.world_to_pixel_values(lon,lat,8657 s); 186/187 steps reached; raster steps
            80-100 -> SJI frames 23-37 (time-nearest truth 26-33; spatial overlap of the 6-arcsec cutout explains the extra frames). Profile default: 'ProfileViewer default x_att: Helioprojective Longitude | x_att_pixel: Pixel Axis
            0 [z] | function: maximum'; after x_att=world Wavelength, x_display_unit='Angstrom' drew with x range 1398.6-1399.3.
      [2]:
        id: P1.4
        agree: False
        correction: Status (not_implemented), feasibility (trivial), the physical-type matching code and the conclusion 'P1.4 is not obsoleted by ape14-wcs-autolink' all stand and the conclusion is stronger than stated. Two evidence claims
            are wrong. (a) 'Fork WCSLink: raster ROI->SJI 0 px' is not a directional limitation: the fork link is bidirectional — a raster ROI on steps 0-15 reaches 18600/91760 SJI px; steps 40-120 give 0 only because the frame-0
            geometry maps raster steps >= 14 outside the SJI frame-0 FOV (a whole-frame SJI ROI reaches raster steps 0-13 of 187). Correct framing: the fork link is exact frame-0 geometry in BOTH directions; LinkSame is per-frame
            exact for raster->SJI and world subsets, and lacks SJI-pixel->raster only because the raster carries no SJI time cid. (b) 'neither link kind is time-aware (that is Phase 3.2)' is wrong for subset propagation: see P1.3 —
            LinkSame + a time ComponentLink + all-ones correlation matrix on the SJI wrapper is time-aware on stock glue (186/187 steps vs 14/187). Also fix the magnitude statement: the sns test observation is a real rotation-
            tracked 4.8 h sit-and-stare (aux XCENIX -53.3 -> -11.1 arcsec, ptp 42.2; PZTX constant), not a cropping artifact, so on a full-frame SJI (0.16635 arcsec/px per the gWCS cdelt) the fork's frame-0 link is ~250 px off by
            the end — Phase 3.1 autolink is a first-order link only for short rasters and cannot replace the helper.
        evidence: Stock glue: LinkSame by physical type -> 14 dc.links, world subset SJI->raster 111360/216920, raster ROI->SJI 19380/91760, SJI ROI->raster IncompatibleAttribute (vc_roi.py linksame re-run). Labels confirmed: SJI world
            cids ['Time (Utc)','Latitude','Longitude'] (inner gWCS names win at loaders/iris.py:38-43), raster ['Helioprojective Longitude','Helioprojective Latitude','Wavelength']; docs/user_guide/loading-iris-level-2-raster-and-
            sji-data.rst:57-59 names 'Helioprojective Longitude' for both — docs correction agreed. Stock wcs_autolink: 0 links. Fork (own detached worktree at 016d9cf2, PYTHONPATH): pytest glue/plugins/wcs_autolinking -> 31 passed,
            1 xfailed, 1 xpassed; 1 WCSLink SJI (x,y) <-> raster (slit,step); SJI ROI->raster 1740; raster ROI steps 40-120 -> 0 but steps 0-15 -> 18600 (scratchpad/sv_timelink.py fork, sv_reach.py); link output == value route at
            t=0 exactly ([21.1,88.6,225.8],[5.9,20.7,35.3]); docstring wcs_autolinking.py:110-113 'exact at the first exposure'. Time dependence: px(10,10) lon -54.73 at t=0 vs -12.51 at t=61; inverse round-trips
            (lon61,lat61,t61)->(10,10,61). fork+linksame coexist (3 external links, all three propagation tests as expected). Note the auditor's vc_acc.py crashes at line 31 (sji['Time'] does not exist — _cube_data only adds Time
            from extra_coords, which the SJI lacks), so its 'nearest SJI exposure' comparison never ran.
      [3]:
        id: APE14-TODO-4.2
        agree: True
        correction: 
        evidence: Reproduced on stock and fork: _GlueWCS(SJI) celestial unit (arcsec,arcsec); _GlueWCS(raster) celestial unit (deg,deg) while values are arcsec. It is worse than 'incoherent by 3600x':
            HighLevelWCSWrapper(_GlueWCS(raster)).pixel_to_world(0,20,93) raises ValueError 'Latitude angle(s) must be within -90 deg <= angle <= 90 deg, got -399.83 deg' (low-level values [-399.83, -31.82] arcsec). The 3-line
            world_axis_object_classes override to arcsec is the right fix; add a test that the high-level call no longer raises. Relevant beyond IRIS pairs: the fork's path-1 (pixel_to_pixel via SkyCoord) would hit this exception
            for raster <-> any astropy-WCS helioprojective dataset (sunpy AIA map, plan P2.4) — check the fork catches ValueError, not only IncompatibleWCS.
    summary: All four statuses hold (nothing in glue-solar; stock glue 1.27 APIs as described; fork tests 31 passed/1 xfailed/1 xpassed; stock autolink 0 links). I refuted two pieces of the auditor's reasoning that change the plan:
        (1) 'neither link kind is time-aware, coordinated browsing waits for Phase 3.2' — on stock glue, LinkSame(lon,lat) + ComponentLink(raster Time -> SJI 'Time (Utc)' world cid) + a _GlueWCS subclass for the SJI with an all-
        ones axis_correlation_matrix gives time-aware SJI-ROI -> raster propagation (186/187 raster steps vs 14/187 for the fork's frame-0 WCSLink; derived (x,y,t) equals the direct gWCS inverse), ~20 lines in glue-solar; root
        cause is glue's dependent_axes (coordinate_helpers.py:163-175) reading the forward correlation matrix for the inverse direction, not an uninvertible gWCS. Only slider synchronization remains upstream Phase 3.2; the
        plan's 'time-nearest join primitive' in glue-core is unnecessary for subsets. (2) 'fork WCSLink gives no raster->SJI' — it does (18600 px for steps 0-15); what it gives is frame-0 geometry both ways, which on this real
        rotation-tracked 4.8 h sit-and-stare (XCENIX drift 42.2 arcsec) reaches only raster steps 0-13 — ~250 full-frame SJI px of error at the end — so the auditor's bottom line 'P1.4 is not deleted by Phase 3.1' is right and
        stronger than stated; plan P1.4/P3.1/order-row-2 and APE14_AUTOLINK_TODO §4 bullet 3 need the 'deleted once autolink lands' text removed. Also missed: ProfileViewer default x_att is the raster's axis 0 ('Helioprojective
        Longitude'), so the P1.3 helper must set x_att='Wavelength' (x_display_unit='Angstrom' works); TODO §4.2 is a hard crash (ValueError on HighLevelWCSWrapper(_GlueWCS(raster)).pixel_to_world), not a silent 3600x; the
        auditor's vc_acc.py crashed at line 31 (SJI Data has no 'Time' component) so its nearest-exposure figures never ran. Environment: ~/.glue/settings.cfg now has unit_converter = \"default\" (08:19), so the auditor's 'iris-
        velocity' warning is resolved; glue/_version.py untracked in the glue fork is vcs-versioning output dated Aug 31 (pre-existing); sibling worktrees wt-glue-pw and wt-glueqt-pw remain in the scratchpad; I removed my wt-
        ape14-sv; glue, glue-qt, glue-solar working trees unchanged. My scripts: scratchpad/sv_timelink.py, sv_timeaware.py, sv_reach.py, sv_slider.py.