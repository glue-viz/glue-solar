**Scripts (all in the scratchpad, prefix `cc_`):** `cc_collapse.py`, `cc_region.py`, `cc_rewrap.py`, `cc_timelink.py`, `cc_forkmap.py` (ran against a throwaway worktree of glue `ape14-wcs-autolink`, removed afterwards), `cc_stackwcs.py`; plus re-runs of `v_qt.py`. Repos untouched (`git status`: only the pre-existing untracked md/_version files).

## 1. Rows/items not covered by any finding

- **Plan L38 / Audit L172 Detector view** ("full-detector loader variant", "detector-aware scales", "linked row/column plots"): no finding. Not checked by anyone; irispy 0.8.1 `read_files` is per-window only.
- **Plan L40, L186-188 / Audit L174 per-scan WCS in the 4-D stack** ("needs glue-core slice-dependent WCS support"): no finding tested the premise. Verified (`cc_stackwcs.py`): `gwcs` + `astropy.modeling.tabular.tabular_model(3)` over stacked -TAB lon/lat tables reproduces every scan's own -TAB coordinates exactly (dlon = 0.0 for scans 0-2, ~30 loader lines, forward). Catch: `gwcs.WCS.numerical_inverse`/`world_to_pixel_values` fail ("too many values to unpack") — a custom inverse model is needed for links/subsets. So the non-goal's premise is wrong: glue-core needs nothing; the blocker is a loader-side inverse model (medium).
- **Plan L41 / Audit L175 sit-and-stare chunking**: no finding. The irispy `sns` test raster IS sit-and-stare and already loads as `(187, 40, nwl)` with axis 0 = time (viewer-composition verifier); whisker preset gives the "solar-y vs time" layout; chunking = glue slices/subsets. Row is effectively covered-by-default; neither doc says so.
- **Plan L35 / Audit L169 OBS metadata**: non-goal, unaudited. `irispy.obsid.ObsID(3860258481)` prints description, SJI filters, FOV, exposure, binning, cadence, linelist (verified) — free improvement of the Partial row, not mentioned anywhere.
- **Audit L88-90, L70/72/73, L171 "snapshot or sequence export", "animation", "PostScript/JPEG output", "IRIS-specific export workflow"**: no finding; plan L163-164 claims only current-slice export exists. Verified: no movie/animation/ffmpeg/imageio export anywhere in glue-qt or glue-core (grep empty; `glue_qt/plugins` = dendro_viewer, tools). Sequence export is a real, unlisted gap.
- **Plan L110-112 "prefill wings from a Profile-viewer wavelength subset" / Audit L91 `xmoment_moment`**: unaudited. Interaction: on fork profile-wcs in WCSAxes mode `apply_roi` builds the subset on `x_att_pixel`, so the follow-up must accept pixel-index ranges, not only wavelength lo/hi. See also §3.1 (the Collapse tool already has this range UI).
- Audit L152/L173 "1-D profiles" only via profile-wcs; fine. Everything else in both tables/phases has a finding.

## 2. Cross-area contradictions (verified)

- **P1.4 fate.** ape14 `P3.1-sec4` ("drop Phase 1.4 now") and doc-3 auditor vs viewer-composition P1.4 auditor+verifier ("complementary; fork WCSLink is frame-0 geometry, ~250 px off after 4.8 h; keep it"). Viewer-composition is right (sv_reach: 14/187 raster steps reached by fork link vs 186/187 time-aware). Keep P1.4 as `link_hpc` by physical type; 3.1 adds the pixel link.
- **Time-aware SJI ROI via all-ones `axis_correlation_matrix`.** viewer-composition verifier recommends it (sv_timeaware.py, "viewer still draws"); slit-overlay verifier says it breaks glue-qt `ImageViewer.format_coord`. Re-ran `v_qt.py`: PATCH=0 → `−33" −400" (world)`; PATCH=1 → `OverflowError cannot convert float infinity to integer`. slit-overlay wins; the only safe form is the ~10-line wrap of `glue.core.coordinate_helpers.world2pixel_single_axis` (rebound in `component_link`) — a glue-core monkeypatch, so viewer-composition's "time-aware in 20 lines" is "small, hacky, belongs upstream".
- **P1.6-dep as prerequisite.** slit-overlay auditor: prerequisite for P1.3/P1.4. Its verifier + viewer-composition: not for lon/lat-only links (raster→SJI works today); only once a synthesised time link exists. Verifier right.
- **Moment-map linking after 3.1.** science-moments verifier: "pixel LinkSame stays required even after 3.1 (fork raises IncompatibleWCS for raster3D↔map2D and map↔map)". Root cause is `APE14-TODO-4.2`/`P3.1-sec4` (deg object classes). Verified on the fork with the coherent `FixedGlueWCS` (`cc_forkmap.py`): raster3D↔map2D autolink = 1 (slit/step ↔ x/y), map2D↔map2D = 1; stock `_GlueWCS` = 0/IncompatibleWCS. The glue-solar coherence fix makes P2.1-link automatic on 3.1; explicit LinkSame needed only on released 1.27.
- **Coherence-fix size.** viewer-composition "3 lines, `world_axis_object_classes` only" vs ape14 "~15 lines, also `world_axis_object_components` getters". The getters are used by `world_to_pixel` (`high_level_objects_to_values`); the 15-line version is what autolinked above. 3-line version fixes only `pixel_to_world`.
- **P3.2 stopgap vs P3.6 sessions.** sync-slicing's closure `ComponentLink(using=lambda)` is unserializable (verified by its verifier) while P3.6 says sessions are ~60 lines away. Resolution verified (`cc_timelink.py`): precompute nearest indices as int32 components on both datasets, identity `ComponentLink([sji.id['nearest_exposure']], ras_pix0)` + `JoinLink` on the same component; `sji[ras_pix0, (3,0,0)] → 5`, `GlueSerializer` round-trips (6.7 KB), translation works after reload. Caveat: `translate_pixel` refuses ("Dependency on non-pixel component") — the sync callback must use the `data[cid, view]` idiom the prototypes already used.
- **P2.1 UI hook.** science-moments recommends `layer_action` and "open an ImageViewer on velocity"; viewer-composition: `layer_action` callback gets `(layer, data_collection)` only. Confirmed (`UserAction._callback(layer, dc)`; `Session.application` is a weakref never passed; only `menubar_plugin(session, dc)` reaches the app, as `browse_iris` does). Choose: layer_action without auto-opening a viewer, or menubar_plugin + dataset picker.
- **Cube-handle prerequisite (plan L113-115, P2.1-prereq "needed") vs science-fitting ("not needed").** Verified for moments too (`cc_rewrap.py`): `SpectrogramCube(d[cid], d.coords._wcs, None, u.Unit('DN_IRIS_NUV') under add_enabled_units(DN_UNIT), d.meta, mask=d['… mask'])` → `calculate_moments` runs (5 maps, ingest OK). The prerequisite is unnecessary; zero loader changes for the moments PR (stacks: dict meta → TWAVE fallback).
- **Environment notes are all stale.** Five auditors reported `unit_converter = "iris-velocity"` breaking `import glue` (caused and repaired by the units-lines-blink auditor); the macOS verifier then flagged `font_size = 9.0`. Current `~/.glue/settings.cfg` (08:36): `font_size = -1.0`, `unit_converter = "default"`. Nothing to act on.
- **Fork readiness wording.** doc-2 says profile-wcs/ape14 are "tests green, awaiting PR"; profile-wcs verifier found 3 RUF059 lint failures, ape14 verifier 2 PLR0917 hits on current ruff, and glue-qt B carries a gitignored `_version.py`. "Green" is pytest-only; both need lint fixes before PRs.
- profile-wcs auditor's "SJI inner WCS is astropy, not gWCS" — refuted by its verifier; everyone else agrees on gwcs.

## 3. Capabilities all auditors missed

1. **glue-qt Profile viewer "Collapse" tab + `glue.core.aggregate.mom1/mom2` + `AggregateSlice`** (`glue_qt/viewers/profile/profile_tools.py:31-38, 302-331`; `glue/viewers/image/state.py:51`). Draw a wavelength range in the Profile viewer → the Image viewer displays Sum/Mean/Median/Min/Max/Moment 1/Moment 2 over it, live, zero plugin code. Verified (`cc_collapse.py`): `AggregateSlice(slice(10,40), 20, np.nansum)` on the Mg II k raster gives `(8,109) == nansum(data[:,:,10:40])`. Changes audit rows L178-179 from Missing to Partial (pixel-unit moments, no rest wavelength/velocity/masking), and it IS the `xmoment_moment` "define the line interactively" UI the plan defers at L110-112.
2. **`irispy.obsid.ObsID`** — see §1.
3. **gwcs + `tabular_model(3)` per-scan stack WCS** — see §1 (forward exact; inverse is the real work).
4. **Checked and rejected: glue-core `RegionData`** (shapely 2.1.2 installed; glue-core requires shapely≥2) for the FOV polygon/slit. Renders only when the target Data has `coords=None`; with the sunpy-map WCS Data it crashes (`data_region.py:219 → coordinate_helpers.py:42 AttributeError: 'tuple' object has no attribute 'shape'`) — glue-core bug. The PolygonalROI subset (network verifier) stays the answer.
5. **glue-core datetime-axis bug already hits shipped data.** Every raster/stack Data carries a datetime64 `Time` component; `glue/utils/matplotlib.py:449` uses epoch 0001-01-01 vs matplotlib 3.11's 1970-01-01, no `set_epoch` anywhere → any Scatter/Histogram of `Time` mislabels dates today, not just the GOES plot.
6. No sequence/movie export exists anywhere (§1) — worth a plan row, since `iris_ximovie` export was the audit's explicit item.

## 4. Hidden dependencies / unreleased upstream

- **Ships on released glue-core 1.27.0 + glue-qt main, no fork:** P1.1, P1.2 (whisker only), P1.4 (`link_hpc` by physical type), P1.5, P1.6 (subset from `SLTPX1IX`), P1.7, P2.1 (rewrap; one Data + 5 components + 2 pixel LinkSame; menubar hook), P2.2, P2.3a, P2.4 (subset FOV), P3.2 stopgap (component-based nearest link; needs an SJI `Time` component in `_cube_data`; no viewer-added hook → rescan viewers lazily), P3.3 (`unit_converter.members['default']` override + TWAVE fallback for stacks), P3.4 (style-widget dict keyed on `QThreadedProfileLayerArtist`), P3.5 blink, P3.6 (glue-solar savers + v2 `VisualAttributes` saver).
- **Fork-only (unreleased):** 3.1 autolink; profile `'slice'` + WCSAxes (glue profile-wcs); glue-qt sliders (B). Anything assuming a single-pixel spectrum (P1.3 iris_raster_browser-style profile, "linked row/column plots") depends on profile-wcs C being released; on 1.27 fall back to `function='mean'` and gate on `hasattr(ProfileViewerState, 'slices')`.
- **Chains:** P1.3 ← P1.4; SJI→raster ROI in P1.3 ← synthesised time link + world2pixel wrap (§2). P2.1-link on fork ← `_GlueWCS` coherence fix (§2) — do that fix in glue-solar now; it also unblocks raster↔raster and the AIA astropy-WCS path in P2.4 (catch `ValueError`, not only `IncompatibleWCS`). P3.1 PR ← keyword-only tweak (PLR0917); glue-solar regression test ← a glue release. profile-wcs PRs ← 3 RUF059 fixes, reorder C→A, drop `glue_qt/_version.py`.
- **`_GlueWCS.world_axis_units` wavelength m→Å** (profile-wcs.A recommendation) ripples into P3.3 native unit, P2.2 wavelength extraction, P3.6 saver tests, existing tests, P1.3 `x_display_unit`; decide once, before P2.x.
- **P3.4 ↔ P3.6:** `layer_artist_maker` layers are not restorable without their own saver; P3.6's "sessions work" needs a LineList saver or drop-on-save.
- **P3.2 ↔ P3.6:** closure links break sessions — use component-based links (§2, verified).
- **New deps:** P1.5 `sunpy[timeseries]` (h5netcdf); P2.3b fiasco+CHIANTI (de-scope); macOS #68 pyobjc (darwin-only; conda-forge follow-up). #68 is independent of everything else.
- **Plan "Suggested order" vs real chain:** the moments PR has no dependency at all (rewrap). The browser-experience chain is `_GlueWCS` coherence fix → `link_hpc` → quicklook / P2.4 / P1.6 fallback. 3.1 PR is independent but pays off beyond SJI↔raster only after the coherence fix.

## 5. Decided items vs findings

- **Moments-only first PR:** still right, now cheaper — no loader prerequisite (rewrap), no new deps, ~100-150 lines. Corrections: audit rows L178-179 → Partial (Collapse tool); "maps as datasets linked to the source" → one Data, five components, two pixel LinkSame (automatic on fork + coherence fix); tests need explicit `rest_wavelength` (all test windows exclude TWAVE); if it must open a viewer, hook = `menubar_plugin`. "Numeric-only wings" consistent; the deferred prefill can read the Collapse tool's `x_range` / `RangeSubsetState` (pixel indices in fork WCSAxes mode).
- **Non-goals:** Level-3 and EIS unchanged (nothing contradicts). OBS XML stays a non-goal, but `ObsID` gives cheap metadata. Per-scan WCS non-goal: premise wrong (glue-core not needed; loader gwcs forward-exact; inverse model is the blocker) — keep deferred, reword.
- **"Session serialization only as a complete unit" (plan L165-171):** refuted — pieces are independent, glue-solar-only ~60 lines, and the `maps.py:30` `preferred_cmap` crash already breaks every glue-solar session on main. Reverse the decision: fix in glue-solar now.
- **Guiding principle 3 ("viewer behaviour belongs upstream"):** contradicted for 3.2-3.6 — all shippable from glue-solar through public registries (`unit_converter`, `layer_artist_maker`, `viewer_tool`, `saver/loader`, `ImageViewer.tools`). Keep it as "upstream later", not a gate. Principle 1 ("thin glue") strains only where the fix is a private monkeypatch: P1.6-dep (`world2pixel_single_axis` wrap) and P3.4 (`_layer_style_widget_cls` dict).
- **"3.2 largest upstream effort" (plan L198):** refuted (small glue-solar).
- **Status paragraph (L10-17):** "starting with 3.1" stale (implemented on fork, lint pending); "first glue-solar work after #44 is moments" consistent; add that 3.1 needs the glue-solar coherence fix to link anything but SJI↔raster.